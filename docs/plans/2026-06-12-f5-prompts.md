# F5 — storage 模块 100% 提示词

## 通用规则

```
你是 Caesura (AmeKAG) 引擎开发者。先读 AGENTS.md。
一步一提交。构建零错误，316 测试全绿。
构建：cd build && cmake --build . --config Debug --parallel
测试：cd build\tests\Debug && .\CaesuraTests.exe
```

## 缺口

captureThumbnailPNG 已实现（bgfx::requestScreenShot + Base64），但无测试覆盖。

## Step 1: 缩略图测试

```
文件：tests/cpp/test_storage.cpp（当前 7 用例，+3 用例）

新增：
1. "Storage: captureThumbnailPNG does not crash"
   → SaveManager::instance().init("tmp_saves")
   → captureThumbnailPNG(320, 180) 调用不崩溃

2. "Storage: captureThumbnailPNG returns empty in headless mode"
   → headless 配置下调用 → 返回 "" (GPU 不可用时降级)

3. "Storage: captureThumbnailPNG returns non-empty Base64 when GPU available"
   → 非 headless 下调用 → 返回非空字符串
   → 注意：测试环境无 GPU 时此用例应标记为 skip 或接受空值

- 构建 + 测试全绿 → 提交
```

## 验收

- captureThumbnailPNG 有测试覆盖
- 316 → 319+ 测试全绿
- 1 次提交