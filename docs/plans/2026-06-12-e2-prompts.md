# E2 — rpc 模块完善提示词

## 通用规则

```
你是 Caesura (AmeKAG) 引擎开发者。先读 AGENTS.md。
一步一提交。构建零错误，306 测试全绿。
```

---

## 缺口

之前标注 Live2D 面板和 Build 按钮未完成。实际代码中已有 fetch 调用：
- Live2DPanel.jsx: 2 个 fetch（GET /api/live2d/models, POST /api/live2d/load）
- AssetPanel.jsx: 1 个 fetch（POST /api/build）
- EditorServer.cpp: 3 个端点已实现

需审查确认前端功能完整性 + 补充端点测试。

## Step 1: 审查 Live2DPanel.jsx

```
执行：
- 读取 web-editor/src/components/Live2DPanel.jsx
- 确认组件挂载时调用 GET /api/live2d/models
- 确认模型列表渲染（ul/li）
- 确认点击模型时调用 POST /api/live2d/load
- 确认加载状态显示（loading/loaded/error）
- 如有缺失，补充实现
- 提交
```

## Step 2: 审查 AssetPanel.jsx

```
执行：
- 读取 web-editor/src/components/AssetPanel.jsx
- 确认 Build 按钮存在
- 确认点击调用 POST /api/build
- 确认结果展示（成功显示路径+大小，失败显示错误）
- 如有缺失，补充实现
- 提交
```

## Step 3: RPC 端点测试

```
文件：tests/cpp/test_rpc.cpp

新增测试用例：
1. "RPC: /api/live2d/models returns JSON array"
   → 启动 EditorServer → GET /api/live2d/models → 返回 JSON 数组
2. "RPC: /api/build returns file info"
   → POST /api/build → 返回 {"success":true,...} 或 {"success":false,...}

注意：测试中 EditorServer 可在任意端口启动，用 httplib::Client 调用。
构建 + 308+2 测试全绿后提交。
```

## 验收

- [ ] Live2D 面板功能完整（列表+加载+状态）
- [ ] Build 按钮功能完整
- [ ] 2 个新端点测试通过
- [ ] 306 → 310+ 测试全绿
- [ ] 3 次提交