# F1-F4 执行提示词

## 通用规则

```
你是 Caesura (AmeKAG) 引擎开发者。先读 AGENTS.md。
一步一提交。构建零错误，测试全绿（277 基线）。
构建：cd build && cmake --build . --config Debug --parallel
测试：cd build\tests\Debug && .\CaesuraTests.exe
```

---

## F1: CI 三端验证

```
推送 master 到 GitHub → 观察 Actions → 根据失败日志修复 → 三端全绿。

常见修复点：
- macOS: brew 依赖、Metal 后端宏
- Linux: apt 依赖、SDL3 源码构建
- 每修复一次提交一次推送一次
```

---

## F2: 测试最后补齐

```
### F2.1: tests/cpp/test_di.cpp

BackendRegistry 完整测试：
1. 单例访问 &a == &b
2. setRenderDevice/getRenderDevice 往返
3. registerNullBackends 后所有 getter 非空
4. setLuaState/getLuaState 往返
5. tryAlloc/release 不崩溃
6. ResourceHandle 生命周期
7. invalidateHandles 使 handle 失效
8. 未注册 getter 返回 nullptr
(8 用例，更新 CMakeLists)

### F2.2: tests/cpp/test_minigame.cpp

MiniGame 接口测试：
如果 tests/cpp/test_mini_game.cpp 已存在且在运行，则扩展。
否则新建：
1. NullMiniGameBackend 各方法安全默认值
2. BgfxMiniGameBackend 构造不崩溃
3. IMiniGameBackend 接口完整性
(3+ 用例，更新 CMakeLists)
```

---

## F3: Demo 成为可跑 Galgame

```
### F3.1: 扩展 demo_story.ks 为完整脚本

*start → 标题 → bg → 对话 → bgm → choice 分支 → 两个结局 → end
(50+ 行，使用占位符资源路径)

### F3.2: Demo 执行时资源缺不缺都安全

KAG 命令引用不存在的 bg/audio 文件时不崩溃，打印日志跳过。

### F3.3: tests/cpp/test_demo_e2e.cpp 扩展

Demo 执行 30 帧不崩溃 + 触发 text/bg/choice 命令 + 跑到 @end
```

---

## F4: 编辑器前端收尾

```
### F4.1: Live2DPanel.jsx

组件挂载 → GET /api/live2d/models → 显示列表 → 点击 → POST /api/live2d/load

### F4.2: AssetPanel.jsx

添加 Build 按钮 → POST /api/build → 显示结果
```