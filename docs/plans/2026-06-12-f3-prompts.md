# F3 — rpc 模块 100% 提示词

## 通用规则

```
你是 Caesura (AmeKAG) 引擎开发者。先读 AGENTS.md。
一步一提交。构建零错误，316 测试全绿。
```

## 缺口

test_rpc.cpp 有 8 个用例，但全是生命周期测试（构造/start/stop/接口完整性）。无 HTTP 端点功能测试。EditorServer 已注册 7 个端点，需逐一验证。

## Step 1: 端点功能测试

```
文件：tests/cpp/test_rpc.cpp（+6 用例）

新增用例：
1. "RPC: /api/ping returns status ok"
   → 启动 EditorServer → HTTP GET /api/ping → {"status":"ok"}

2. "RPC: /api/status contains lua field"
   → GET /api/status → JSON 包含 "lua" 字段

3. "RPC: /api/assets filter by type image"
   → GET /api/assets?type=image → 返回 JSON 数组

4. "RPC: /api/assets filter by type script"
   → GET /api/assets?type=script → 返回 JSON 数组

5. "RPC: /api/run executes simple Lua"
   → POST /api/run with body {script: "return 42"} → HTTP 200

6. "RPC: /api/logs returns array"
   → GET /api/logs → 返回 JSON 数组（可能为空）

实现要点：
- httplib 已在 external/cpp-httplib/ 目录（header-only）
- 测试中 EditorServer 在随机端口启动，用 httplib::Client 调用
- 每个用例独立 start/stop EditorServer
- 提交
```

## 验收

- 316 → 322+ 测试全绿
- 7 个端点中至少 6 个有功能测试
- 1 次提交