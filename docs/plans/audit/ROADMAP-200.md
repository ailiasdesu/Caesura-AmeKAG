# 200 轮迭代路线图 · 阶段 G：产品化（round 101 起）

> 承接 ROADMAP-100.md（round 1-100 完成）。阶段 G 目标（依据 engine-market-analysis-100-rounds.md §7/§8）：
> 补齐技术短板（真机验证、后处理特效、内容创作体验、资产兼容、生态种子），交付示例游戏并验证引擎完整形态运行表现。
> 门禁纪律与工作流同 100 轮冲刺：全量门禁每轮必跑、子代理并行、语义提交、节点 push + 三平台 CI。

| 轮次 | 内容 | 门禁 | 标记 |
|---|---|---|---|
| 110 | **示例游戏收尾 + 文档站 + 发布就绪（3 子代理，A 失败主代理接管）**：①**i18n 打磨（R110-A 失败，主代理接管）**——**发现并修复真实内容 bug：story.ks 场景 1 的 {items} 复数键在 [ch] 渲染出字面 {n} 残留**（i18n.expand 把 {items}→"{n} 个条目"但 {n} 无填充者——plural 需显式 translate("items",{n=N}) 调用，[ch] 管线不提供；R110-A 探针证实运行时无 {n} filler）；修复：两处台词改 ${tf.letters} 内插 + 内联复数表达式（"1 item"/"3 items" 经 schema.coerce 插值管道验证正确；plural 机制演示由 test_i18n 的 translate API 测试承担）；i18n 键覆盖审计：全部 {key} 引用（items/settings/investigate_sigh/final_choice/end_*_a-d）在 zh/en 词典均存在（BOM 致 loadfile 失败系引擎专用 loader 处理，运行时无感）；example_game README 重写（旧作 The Last Letter → 《单程回信》：能力清单/结构/校验）②**README 文档站整理（R110-B）**——顶层 README：文本徽章（平台/测试/119 契约/31 接口，不依赖外部 CI）、示例游戏节、5 类文档索引（阶段 G 8 新文档全补录 🆕）、示例库清单表（tutorial 01-16+showcase+example_game+sma/galgame/full_pipeline）、数字一致性修复（78→119 契约、接口表 29→31 行补 IMeshRenderer/MobileAdapter、基线 605/2968/52→976/131+24/297/530）；44 链接零断裂；**发现 4 个既有孤儿文档**（galgame-engine-readiness-audit/gpu-api-guard/bgfx-shader-binary-repack/kag-neogenesis-modernization，零引用，记录待处理）③**发布就绪度检查（R110-C）**——docs/guides/sample-game-release.md：GitHub Releases vs itch.io 双路径 + 8 组检查清单全实测（Release 构建零错误、C++ 976/976、verify 5/5、CPack 87.9MB/386 文件、解压冒烟 D3D11 启动干净退出）+ 账号依赖标注（GitHub gh 已认证零动作、itch 需用户账号）；**发现必修阻碍：verify_sample_game.sh 默认路径仍 story.ks.new（round 102 已改名）——主代理修复默认值 + sample-game-verification.md .new 提法 + 清理 _dsh_* 临时文件**；验证脚本裸跑 5/5 PASS | Lua 131/131, 孤儿 24/24, verify 5/5, C++ 976（Release 复验）, 构建零错误 | (round 110 提交) |
| 109 | **Web 部署链路 + 性能基准（3 子代理全收敛）**：①**deploy-web.yml**——workflow_dispatch 手动触发（game 输入，缺省 demo/galgame_demo.ks）→ checkout/setup-node/npm ci+vite build → **安装 lua5.4（关键坑：package_game.sh 硬编码 Windows lua.exe，Linux workflow 无 lua 命令即 FATAL——本地 git bash 验证不暴露）** → gen-index 守卫 → package_game.sh --no-web-build（游戏专属 bundle）→ configure/upload/deploy-pages 三件套；permissions 三件套 + concurrency + environment url；PyYAML 校验通过；packaging-ux.md 补 GitHub Pages 自动部署节 ②**Web 性能基线**——web/perf-baseline.test.js 6 断言（预算留 2x 余量）：示例游戏 story.ks 主路径 2.75 frames/ms（2607帧/949ms）、token 0.35/ms、堆增 383KB（<1024KB）；合成 1000 行 5.87 frames/ms；**规模化线性 2.03x（2000 行 vs 1000 行，捕获超线性爆炸）**；bridge.js +5 行门控帧计数钩子（默认关闭零影响）；perf 文档 round 109 段；**wasmoon 限制记录**：整场同步单线程（lua.doString 阻塞主线程）、协作协程唯一并发、内存= Lua 自管堆（Emscripten 线性内存不可测）、帧=scheduler tick 非 rAF、每场景重建 ctx ③**bundle vs source 性能**——web/perf-bundle.test.js 3 断言：**bundle 路径更快**（微场景 2.0 vs 2.5ms、story.ks 773 vs 874ms 1.13x、1400 token 279 vs 586ms 2.10x）——根因：source 每 runScene 全量 compiler.compile（宏内联/表达式 AOT）而 bundle deserialize 还原 _compiled 命中短路径跳过编译；反序列化/协程重建量化排除；可选优化记录（runFromBundle 每调重建全包，会话内缓存为延迟优化） | Lua 131/131, 孤儿 24/24, web 297（20 文件）, 耦合/覆盖 PASS | (round 109 提交) |
| 108 | **编辑器零代码搭建第一步 + 一键打包分发（3 子代理全收敛 + 主代理契约违例修复）**：①**Scene Builder 面板**——editor/src/ide/SceneBuilder.tsx（挂 App.tsx 面板栈）+ lib/sceneBuilder.ts（纯 .ks 行模板 + DEMO_ASSETS 静态清单）+ store 扩展 editorCursor/setCursor/insertAtCursor + resolveInsertLine 纯函数 + EditorArea 光标实时上报：三类操作（背景 [bg storage=]/立绘 [csp storage= x= y=]/文本 [ch name= text=] + [p] 分页），插入到光标行上方（无光标追加末尾），插入后 setInspected+reveal 联动 Inspector，文档 mark dirty；无活动文档空态；**主代理修契约违例（R108-C 发现）：[csp] 必填 name= 而生成行缺失——buildSpriteLine 加 name 参数（默认 Hero）+ UI name 输入框**；editor 506→530（+24：SceneBuilder 11/store 10/EditorArea 1）+ tsc 干净；EditorArea onDidChangeCursorPosition 触动两处 mock 契约已补 ②**一键打包**——scripts/package_game.sh（set -euo pipefail 6 步：解析输入→ks_check 契约门禁→ks_bake --web→组装 web/dist+scripts+资产+游戏专属 bundle→MANIFEST→--release 指引）+ docs/guides/packaging-ux.md（itch/GitHub Pages/Netlify 分发）+ web/package.json 补 build script + .gitignore /dist/；实测 demo/example_game 全链路 exit 0 + HTTP 冒烟（/ 200、story.lua 41679B 游戏专属）；修 ks_bake --dir Windows dir 依赖改显式路径列表 ③**RPC 桥**——/api/assets 既有端点扩展（+fg 槽位 + kind 字段，EditorServer.cpp）；生成行校验复用 LSP（/api/eval + diagnostics 零改动）；test_rpc +1 用例 26 断言（C++ 975→976）+ editor-api-reference 同步 + scene-builder-rpc-bridge.md 复用手册；真实引擎 HTTP 实测资产发现+LSP 校验正确 | C++ 976/976（8858 断言）, Lua 131/131, 孤儿 24/24, editor 530/530, 耦合/覆盖 PASS, 构建零错误 | (round 108 提交) |
| 107 | **声明式布局 [layout]（hbox/vbox/grid 最小子集）——3 子代理全收敛 + 主代理注册/统一**：①**布局数学与命令族**——scripts/kag/layout_math.lua（238 行纯函数零依赖：hbox/vbox distribute_length 固定+flexible 均分 leftover、cross 轴 align start/center/end、grid 均匀 cell + gridrowcol、slot_rect 越界 nil、空容器 0x0）+ scripts/kag/commands/layout.lua（[layout name= kind= gap= padding= align= cols= w/h/x/y=] + [layout_slot parent= layer= index= size="WxH"] + [layout_place parent= layer= x= y=] 三命令）；**设计决策：容器是"计算器"不是新渲染层**——recompute 后 layers.move_layer 写现有层 x/y，渲染管线零改动，与 [position]/[tween] 天然组合；弃用 [layout slot] 融合拼写（coerce 强制 kind 必填）改独立命令名 ②**settings 迁移试点（可行性验证）**——settings.lua +81/-12：3 总线音量滑块行从手写 startY+(i-1)*lineH 改声明式 vbox（_volumeBand + _rowGeometry 统一驱动渲染/点击/光标三处），**坐标等价证明逐像素一致**（1280x720 面板 680x420 居中：声明式 y=200/230/260 与历史公式全等）；test_settings_layout_pilot.lua 10 断言；**R107-C 发现并行重复实现后删除自建 scripts/layout.lua，统一消费权威 kag.layout_math**（主代理裁决采纳）③**Web parity**——坐标快照路径追踪确认 web 零适配（layout 只写 Lua 层 x/y → jsLayers → core.moveLayer → DOM 渲染器）；layout.parity.integration.test.js 4 用例（mount 真实栈/hbox3+vbox2+grid2x2 DONE 无 error 且坐标精确命中/Lua 侧 measure 同一数学断言桌面 Web 一致/DOM 定位）④**主代理注册**——kag.lua 合并 layout 命令族、init.lua 预载 layout+layout_math（settings 消费）、run_orphan 登记 test_layout_cmds+test_settings_layout_pilot（孤儿 22→24）、web/scripts-index.json 重生成 74→76（修 gen-index 守卫与 player-settings parity 两红）；测试：test_layout_cmds 54 断言（hbox/vbox/grid 矩阵/gap/padding/align 边界/空容器/越界/schema 契约/与 tween+position 组合）；settings 全量回归绿（test_settings 4/4 + config_deep 74/74） | C++ 975/975, Lua 131/131, 孤儿 24/24, web 289（18 文件）, 耦合/覆盖 PASS, 构建零错误 | (round 107 提交) |
| 106 | **声明式补间 [tween]（对标 ATL 最小子集）——3 子代理全收敛 + 主代理注册/修复**：①**契约与实现**——scripts/kag/commands/tween.lua：[tween target= attr=x|y|alpha|scale from= to= dur=100-30000 delay=0-30000 ease=linear|ease_in|ease_out|ease_in_out|back_out wait=true]；from 可省（缺省=当前值）、from/to 支持 ${expr} 插值（schema interpolate 仅 string 生效故 string+interpolate+handler tonumber）；新补间管理器 ctx.tweens（单时间线 delay 相/t 累计/终点精确落 to）；阻塞 wait=true 与 [wait] 同构（Operation+yield，stop_flag/_next_index 提前退出）；非阻塞 wait=false fire-and-forget；kag_runner.update 每帧钩子（pcall+空表短路零开销）；scheduler.lua 零改动 ②**主代理注册**——kag.lua 合并 handler、kag/init.lua 预载、ks_check pcall 列表；**套件顺序坑再现：test_tween 创建全局 mock 须入孤儿套件**（主套件沙箱锁全局）③**Web parity**——阻塞路径天然共享（coroutine.resume 传 16ms dt 契约）；fire-and-forget 在 runScene/runFromBundle 双帧循环各加防御性驱动（镜像 kag_runner 钩子）；**修 R106-A 引入的 web 全挂：scripts-index.json 未重生成（73→74 加 kag.commands.tween）**；新 tween.parity.integration.test.js 3 用例（阻塞 DONE+端点贴合/ease_in vs linear 中段数值正确/非阻塞精确端点）；**主代理修预存失败**：①示例游戏 [shake]/[quake] 在 web jsStubs.vfx 缺 stub → 补 no-op；②autoClick 上限 100 次点击不足（新作 446 行远超）→ 10000（防死循环语义保留）；③flow 测试 story.ks terminal 更新为新作结尾；web 282→285 ④**tutorial_16**——demo/tutorial/tutorial_16_tween.ks（150 行：基础/5 缓动同屏对比/from 缺省/非阻塞+[wait]/表达式三元/delay/alpha+scale），ks_check 零警告 + ks_bake 通过；kag-language-tour §19.7 速查 + sample-library 教程表 ⑤**测试**——test_tween.lua 28 断言（契约默认/必填/枚举拒/clamp、非阻塞 50ms 推进+终值、5 缓动端点矩阵、from 缺省、delay 分相、阻塞协程两段驱动、表达式插值、scale）；孤儿 21→22；ks_check 21 demo 场景零警告 | C++ 975/975, Lua 131/131, 孤儿 22/22, web 285/285（17 文件）, 耦合/覆盖 PASS, 构建零错误 | (round 106 提交) |
| 105 | **示例游戏《单程回信》填充完成 + 端到端验证（3 子代理全收敛 + 主代理两处修复）**：①**场景 3-5 + 三结局 + credits 填充**（story.ks 446 行，替换旧 The Last Letter 为正式示例游戏；旧版保留 story_lastletter.ks）：场景 4 追查（信任差分 [if f.trust>=2]/[add]/[sub] 线索/[while] 翻档案 3 迭代/[csp]/[csl]/[csd] 潮老师闪现/[preload]/[save slot=2]/[unlock type=cg]/分支 B 双层）、场景 5 雨夜（[flash]/[quake]/[shake]/[vib]/[vibrate] 雷雨/[scroll] 日记/[nameplate]/SMA 信使幻象 iscript pcall 惰性注册 + [sma_play]/[sma_anim]/[sma_variant]/[sma_ik]/[sma_stop] headless 全 inert/[until exp=f.lightning timeout=6000] 计时/[button cond=f.trust>=2] 三选项）、三结局（归零/同行/守约，[ending] 解锁 + notify 提示）+ credits；lang zh/en 各 +16 键；信任经济学三结局全可达 ②**端到端验证设施**——scripts/verify_sample_game.sh + tests/scripts/sample_game_headless.lua（mock callable 绑定，关键：is_voice_playing/is_bgm_playing 必须 false 否则 [playvoice]/[playbgm] 等待死挂）+ docs/guides/sample-game-verification.md；**主代理两处修复**：按钮 target 标签名不匹配（*end_zero vs *ending_zero，label not found 卡死）统一为 *ending_*；credits 的 [history]/[gallery] 阻塞式菜单在 headless 死等——换 [notify] 非阻塞提示；修复后验证 5/5 PASS（主路径 DONE 15687 帧 + 三结局可达）③**ks_check 缺口修复**——[button] 未计入标签引用命令清单（*ending_companion 被按钮引用却报 unreferenced WARN）；NAV_LABEL_REF 加 button ④**测试更新**——test_example_game.lua 适配新故事（宏 scene_intro→scene_open、标签集更新、KNOWN 加 scene_open、require 清单补 character/math/resource、FLOW 补 until/elseif——旧故事没用 [until] 未暴露）；样例全部 demo 场景零警告 ⑤**资产/i18n 审计**——sample-game-assets.md：9 项直接复用 + 6 项占位安全降级（LUT 经 palette 守卫零风险）+ owr_* 键预留表 | C++ 975/975, Lua 131/131, 孤儿 21/21, ks_check 零警告, 验证 5/5 PASS | (round 105 提交) |
| 104 | **CARC 商用级补齐 + 存档安全审计（3 子代理全收敛）**：①**nonce 复用检测落地（前两项核实已在 round 88 落地）**——git log 核实 710fd4fa 已实现 addFile 幂等 update（numFiles=唯一路径数）与 keyLen≠32 全拒；本轮补齐 round 88 仅文档化的 nonce 检测：CryptoEngine 新增 (key,nonce) 成对键注册表（进程级 static + mutex、有界 deque 1024 条 ~45KB、encrypt-only 检测避免误拒合法读取、重复即拒绝 fail-closed、默认开启）、setNonceReuseDetection 开关（关时清表）；+3 用例（同 key 复用拒/新 nonce 过/异 key 同 nonce 过、可关闭、多归档连续写不误伤）；C++ 972→975；耦合 2/4 不变 ②**存档防篡改审计**——docs/design/save-security-audit.md（159 行）：6 维矩阵全中/低无高危——AES-GCM 认证覆盖密文+nonce+key 但元数据/槽位路径不在 AAD（中）、无单调计数器防回滚（中）、槽位仅文件名绑定密文可复制（中）、自定义 provider 完全跳过加密（低-中）、路径逃逸已防御（低）、schema 迁移无注入（低）；建议 P2：GCM AAD 槽位绑定（消除元数据伪造+槽位交换，成本 M）+ 单调计数器防回滚（S-M）；P3：provider 加密强制 ③**CARC 文档重写**——carc-packaging.md 二进制格式图改真实布局（Header 64B/FileEntry 116B/Trailer 96B/索引始终 AES-256-GCM/zstd——原文 128B/可选加密/miniz 头注释均错误）、概述改加密/压缩/签名始终启用、更正"复用密钥打包"示例（每次 pack 新密钥对）；engine-safety 补 CAES 魔数布局与迁移链 v1→v5；021 §5 三行状态更新（去重/密钥/nonce 全落地）；test_carc_import +3 断言（list 64-hex 哈希/extract 还原相对路径/未知路径拒绝）；Lua 131 全绿 | C++ 975/975（8832 断言）, Lua 131/131, 孤儿 21/21, 耦合/覆盖 PASS, 构建零错误 | (round 104 提交) |
| 103 | **Metal/Android/跨平台就绪度审计（3 子代理全收敛 + 主代理去重）**：①**Metal 审计**——docs/guides/metal-readiness.md（169 行）：初始化路径完整（--backend metal → bgfx RendererType::Metal，NSWindow* 与 SDL_COCOA_WINDOW_POINTER 代码级证实兼容；EmbeddedShaders_Metal.cpp 已编入 render 库）；postfx 在 Metal 三重降级（无字节码→fsTexture 恒等→fallbackProgram）绝不空 program 崩；无致命 D3D 假设（2 WARN：默认后端 Direct3D11 靠失败回退不够显式、embed_to_c.py FILES 与 compiled/{macos,linux} 产物 stale）；CI 仅编译级覆盖无运行级——建议：编译日志 grep 断言 Metal cpp 被编译 + embed_to_c stale 守卫 + （更强）--backend metal --frames 30 冒烟 ②**Android 构建链**——docs/guides/android-build.md + scripts/build_android.sh（bash -n 通过）：NDK r26+ toolchain、arm64-v8a、SDL3_DIR 必需（仓库 external/SDL3 是 Windows 预编译包）；**关键发现 R1-R11**：顶层 add_executable 非 .so（JNI 需 MODULE 改造）、默认 D3D11 且 setPreferredBackend 无 'gles' 字符串（须 --backend opengl）、SoLoud 仅 OpenSLES（-DSOLOUD_BACKEND_OPENSLES=ON）、main.cpp chdir 找 assets/（Android 无 CWD）、JNI/Activity 壳缺失、存档 saveDir 桌面 CWD；本机无 NDK 按计划跳过交叉编译（脚本无 NDK 明确报错）；真机清单 8 项待设备 ③**跨平台 CI 强化**——docs/guides/cross-platform-verification.md（136 行三平台×能力域矩阵：CI 已验证 vs 待真机）；风险核对：.dxbc 已有 *.dxbc binary 规则（round 94 坑已防）、EmbeddedShaders.cpp 非条件编译（mac/Linux 也编 DXBC 数组无害）、RenderBinding 无平台假设、isPostFxSupported 后端无关（mac/Linux 真机支持=true 但恒等降级）；ci.yml 结论：无强制改动（编译覆盖充分），可选建议 3 条；**主代理修 EmbeddedShaders.h postfx extern 声明重复两次**（R103-C 发现） | C++ 972/972, Lua 131/131, 孤儿 21/21, 构建零错误 | (round 103 提交) |
| 102 | **后处理特效栈 v1（新 GPU 能力：bloom/vignette/LUT/柔焦）——3 子代理全收敛 + 主代理契约/登记**：①**主代理契约**——IRenderDevice.h 新增 PostFxKind 枚举（Vignette/LutColorGrade/SoftBlur/Bloom）+ PostFxParams + 6 方法（createPostFx/setPostFxParams/destroyPostFx/clearPostFx/isPostFxActive/isPostFxSupported）；Null 全降级（0/false/no-op）；Bgfx 骨架（句柄=index+1、commit_frame 挂钩）②**GPU pass 实现**——runPostFxChain：链激活时 VIEW_MAIN 重定向 m_sceneRtt → 逐 stage 全屏 pass（scratch RTT 乒乓）→ backbuffer；新增 VIEW_POSTFX=40 合成视图（RTT→MAIN→POSTFX→DEBUG→TRANSITION）；bloom 内部多 pass（bright-pass+½ 降采样+¼ blur×2+additive）；fxc 真编译 DXBC 内嵌（vignette/lut_grade/soft_blur/bloom 4 shader），GL/Metal/Vulkan 恒等拷贝降级（.sc/.metal 参考源码 + shaderc 编译说明）；m_sceneRtt 惰性按尺寸重建覆盖 resize，shutdown/resize/recoverDevice 钩资源释放；修 test_live2d/EntryLifecycleBackends/test_rtt_pool 三个 mock 接口适配 ③**[vfx] 命令族**——schema 契约 118→119（postfx enum {bloom,vignette,lut,softblur,none} + strength/radius/amount/lutMix clamp + rgb "r,g,b"；validation-only 不设 default 保 legacy 语义）；分发：postfx=none→clear_postfx、其余→is_postfx_supported 降级 no-op、支持→set_postfx；无 postfx 参数 legacy fade/blur/quake 路径不变；RenderBinding 新增 set_postfx/destroy_postfx/clear_postfx/is_postfx_supported/is_postfx_active（per-kind 句柄缓存、缺省参数映射、api-stats 38 API 自动计入）；backend_factory/backend/sandbox 白名单接线 ④**测试**——test_render_postfx.cpp 9 用例（枚举固定值/Params 默认值/6 方法签名 pin/Null 降级/Bgfx 未 init 门禁；真实句柄生命周期注明需 GPU）；test_vfx_postfx.lua 18 断言（none 关闭/非法拒绝/合法接受/缺省映射/legacy 兼容/headless 降级）——**发现套件顺序坑：创建全局 mock 的测试须入孤儿套件**（主套件沙箱中途锁全局），已登记 run_orphan_tests.lua ⑤**文档**——command-contracts 重生成 119、lua-modules 补 5 API、kag-language-tour 特效节、api-stats 重生成、shaders/README + 编译脚本 + embed_to_c.py | C++ 972/972（+9）, Lua 131/131, 孤儿 21/21（+1）, 耦合/覆盖 PASS, 构建零错误 | (round 102 提交) |
| 101 | **阶段 G 启动：宏缺陷回归锁定 + 大型资源压测 + 示例游戏立项（3 子代理全收敛）**：①**嵌套宏定义收集确认已修复 + 回归锁定**——round 75（23cb8e6a）已同时落地收集侧深度计数（scheduler.lua 1159-1170 bdepth + compiler.lua 378-400 同款 + 含嵌套宏标记 DYNAMIC 排除内联），任务前提过时；本轮补 test_macro_scene D 段 8 断言锁定：outer 体完整收集 4 token（未被首个 endmacro 截断）、inner 惰性注册运行期可用（T:OUTER_BODY,T:INNER_BODY,M:DONE）、无悬空 endmacro 破坏后续流；test_macro_scene 17→25 ②**大型资源压测套件 test_scale_stress.lua（20 断言，登记主套件，Lua 130→131）**——5 维度全设预算且余量充足：A 4096×4096 瓦片图集 texel 记账 3.0ms（<1s）；B 音频句柄池 80000 次 alloc/free 并发上限 128 105ms（<2s）；C 9600 token 大场景 tokenizer 1.59s（<10s）+ scheduler 走满 90ms；D 500 页 backlog 累积堆增长 934.7KB（<4096KB）；E 408,972 字节 3000 行叙述流逐行 translate 1.476s（<10s）；确定性计数断言（瓦片 4096/texel 4096²/句柄复用>1000/帧数==tokens+1/[ch] 派发 4800/无 && 残留）；perf 文档追加 round 101 段；**踩坑记录：string.format 字面量 % 须 %%；340KB 单字符串喂 translate 会 O(n²) 挂起——正确模型是逐行翻译（scheduler 本就按 token 单独翻译）；500 页 backlog 堆增长约 935KB 健壮**；结论：大型资产维度无压力上限撞顶 ③**示例游戏立项《单程回信 The One-Way Reply》**——demo/example_game/DESIGN.md（253 行 8 节）：现代校园温情悬疑短篇，3 角色（主角/澪 Mio/潮 Ushio）、3 结局（归零/同行/守约）、8 流程节点约 17.5 分钟；能力展示清单逐项落点真实命令（文本/立绘/背景/音乐/语音、分支、双存档点、i18n 中英热切换、转场/粒子/后处理占位、循环读信、表达式分支、SMA 小游戏融合、backlog/跳过/自动、[ending] 多结局）；资产复用全部现有 + 6 项设计占位（安全降级）；脚本填充自 story.ks 单文件起步，每轮 ks_check + ks_bake + Web 校验 | Lua 131/131, 孤儿 20/20, C++ 963/963, 耦合/覆盖 PASS | (round 101 提交) |
