# 100 轮自主迭代路线图（campaign 2026-08-14）

> 目标：分析现有情况，自主完成后续的迭代，要求迭代 100 轮，每轮任务不得过于简短。
> 门禁纪律（每轮必跑，与 AGENTS.md §5 一致）：全量构建零错误 → C++ 全绿 → Lua 全绿 → ctest 全绿 → 耦合 PASS → 语义提交。
> 推送策略（用户约定）：多轮本地累积语义提交，到目标节点统一 push + 一次三平台 CI。

## 现状基线（round 1 起）

- 40 轮审计已完成（docs/plans/audit/final-report-40.md）：P0×2/P1×8/P2×23 全部关闭或记录
- 测试基线：C++ 659 例 / Lua 120 例 / ctest 11 项（AI smoke 跳过）/ 耦合 PASS / 三平台 CI 全绿
- 上一轮已关闭：UnifiedBinding 迁移、RTTManager 双池修复、音频 CI 守卫、RTT 池 + 文本缓存测试
- 本轮（round 1）已关闭：TextRenderer 纯布局提取（P2-10 剩余缺口之一）+ 11 例无 GPU 测试

## 剩余已知缺口（后续轮次的素材池）

| 编号 | 内容 | 类型 | 预估轮次 |
|---|---|---|---|
| G1 | BgfxQuadBatch 合并组注入重构 + 测试 | P2-10 剩余 | 2-3 |
| G2 | TextRenderer rebuildCache 脏区间/缓存命中路径测试（updateDirtyRange 纯函数化） | P2-10 剩余 | 2-3 |
| G3 | Live2D/Steam SDK 路径验证文档 + 条件编译测试桩 | P2-6/P2-5 | 3-5 |
| G4 | 编辑器增强：可视化场景树/时间线（在 /api/pick+stats+SMA 基础上） | 追赶路径 | 10-15 |
| G5 | Web 导出可行性调研（emscripten）或移动运行时方案 | 追赶路径 | 5-10 |
| G6 | 示例作品库 + 教程（对标 Ren'Py 教程体系） | 追赶路径 | 5-8 |
| G7 | 性能基线更新（round-25 之后的回归对比） | 维护 | 2-3 |
| G8 | 渲染管线深层优化（quad 合并、脏矩形、atlas 打包） | 优化 | 8-12 |
| G9 | 脚本层：KAG 命令契约文档同步 + 新命令实现 | 功能 | 10-15 |
| G10 | 存档/资源/音频模块边界重构与测试补强 | 硬化 | 5-8 |
| G11 | 平台层：输入映射、窗口管理、事件优先级测试 | 硬化 | 3-5 |
| G12 | 代码质量：clang-format 全量、死代码清理、耦合再收敛 | 维护 | 3-5 |
| G13 | 文档同步：api/cpp-interfaces、lua-modules、能力矩阵 | 维护 | 3-5 |
| G14 | 发布流程：CPack 打包、changelog、release notes 自动化 | 维护 | 3-5 |

## 阶段规划（100 轮）

- **阶段 A（round 1-20）渲染可测试性收官**：G1/G2/G8 前半 + TextRenderer 其余纯函数化
- **阶段 B（round 21-40）编辑器增强**：G4（场景树/时间线/检查器增强）
- **阶段 C（round 41-55）分发与生态**：G5/G6（Web 导出调研、示例库教程）
- **阶段 D（round 56-75）脚本与玩法深度**：G9（KAG 命令/契约/表达力）
- **阶段 E（round 76-90）硬化与性能**：G3/G7/G10/G11
- **阶段 F（round 91-100）收尾**：G12/G13/G14 + 100 轮总结报告

> 阶段划分是指导性的：每轮先看"素材池"，选杠杆最高的任务；若某任务被证明不可行
> （如 Web 导出调研发现需外部 SDK），如实记录并替换为等量价值任务，不硬凑。

## 轮次记录

| 轮次 | 任务 | 结果 | 提交 |
|---|---|---|---|
| 1 | TextRenderer 纯布局提取 + 11 例无 GPU 测试（G2 前半） | C++ 659/659, Lua 120/120, ctest 绿, 耦合 PASS | 4c12c9c5, 6c30d545 |
| 2 | TextRenderer 脏区间纯函数化（countUtf8Glyphs/computeDirtyRange）+ 10 例无 GPU 测试（G2 完成） | C++ 669/669, Lua 120/120, ctest 绿, 耦合 PASS | b36d82c8 |
| 3 | BgfxQuadBatch 批次数学纯函数化（quadToNdc/computeMergeGroups/buildGroupIndices）+ 11 例无 GPU 测试（G1 完成） | C++ 680/680, Lua 120/120, ctest 绿, 耦合 PASS | 96a2cd6c |
| 4 | 共享像素→NDC 纯数学 NdcMath.h，blitTexture/stretchBlt/affineBlt/quadToNdc 统一委托 + 7 例测试（G8 前半） | C++ 687/687, Lua 120/120, ctest 绿, 耦合 PASS | 7c8b2849 |
| 5 | 无障碍颜色滤镜数学纯化（Machado-2009 预设表 + effect-4 VFX 打包）+ 7 例测试（G8 中段） | C++ 694/694, Lua 120/120, ctest 绿, 耦合 PASS | 89aaf4f1 |
| 6 | DirtyRect merge uint16 回绕修复（32 位计算+钳制）+ shouldUseScissorFor 纯决策 + 11 例测试（G8 中后段） | C++ 705/705, Lua 120/120, ctest 绿, 耦合 PASS | 853e0e31 |
| 7 | 粒子生命周期衰减纯化（lifeFade + buildParticleVisual + maxLife=0 NaN 守卫）+ 6 例测试（G8 收尾） | C++ 711/711, Lua 120/120, ctest 绿, 耦合 PASS | 22203ce8 |
| 8 | 确定性测试套件扩展：嵌套 elseif、jump/call 流程、表达式边界（除/模/串/链）、深快照、状态隔离（G9 前哨） | Lua 120/120（+28 determinism 断言）, C++ 711/711, 耦合 PASS | be61f8be |
| 9 | 表达式语言边界测试：括号作用域、表键引号、短路防 nil 解引用、严格类型、数字字面量/双重否定/链式比较（G9 中段） | Lua 120/120（+17 expr 断言）, C++ 711/711, ctest 绿, 耦合 PASS | 81872bae |
| 10 | tokenizer 边界：blocktext 三引号、注释、iscript 原始体、标签、BOM/CRLF/unicode 鲁棒性、未闭合降级（G9 中段） | Lua 120/120（+22 tokenizer 断言）, C++ 711/711, ctest 绿, 耦合 PASS | 754676d6 |
| 11 | 替换 4 个假通过 switch 测试为 7 项真实断言：case 精确路由/无回落、default 回退、缺变量回退、无匹配跳过、嵌套 switch 隔离（G9 中段） | Lua 120/120（scheduler 25/25）, C++ 711/711, ctest 绿, 耦合 PASS | 79514217 |
| 12 | 孤儿测试审计：发现 8 个 test_*.lua 从未进 runner（假绿）；确认全部 PASS 后建独立 run_orphan_tests.lua（与沙箱套件顺序互斥不可合并）+ 修复 os.exit/BOM/label 计数（G9 中段） | Lua 120/120 + 孤儿 8/8, C++ 711/711, ctest 绿, 耦合 PASS | 4d5bff82 |
| 13 | 孤儿测试接入收尾：精确诊断重排失败根因（全局 mock 与沙箱顺序冲突），改用隔离运行器方案；更新 CLAUDE.md 门禁说明（G9 中段） | 同 round 12 门禁 | 4d5bff82 |
| 14 | 防回归机制：check_test_coverage.py（Lua 128+C++ 65 全注册校验）+ CI Test coverage check 步骤——未注册测试=失败（G9 收尾/质量基建） | Lua 120/120+孤儿 8/8, C++ 711/711, ctest 绿, 耦合 PASS | f67399a6 |
| 15 | 文档同步审计：KAG 契约命令数全仓漂移（72/78/81/84 并存），核实权威值 84（schema_doc 自动生成）并同步 README/AGENTS/CLAUDE/kag-neo-genesis-language；规避 PowerShell UTF-16 重定向坑（G9 收尾） | 文档一致性 | f5caf28d |
| 16 | 编辑器测试基建（G4 起航）：vitest + SceneTree parseSceneElements 7 例（标签/类型分类/注释跳过/截断/行号/空源）+ CI npm ci 步骤 | vitest 7/7, tsc 0, ctest 绿, 耦合 PASS | cfd6de2f |
| 17 | 编辑器安全纯函数测试：luaString 长括号转义（注入守卫：终止符形状内容/等号升级/10 连等） + luaValue JSON→Lua（标量/nil/转义/嵌套表）（G4 中段） | vitest 20/20, tsc 0, ctest 绿, 耦合 PASS | 339e2cf2 |
| 18 | EngineClient RPC 客户端测试（可注入 fetch mock）：URL 拼接/token 头/Content-Type/查询参数/JSON 解析/RpcError/404 详情/setBase（G4 中段） | vitest 28/28, tsc 0, ctest 绿, 耦合 PASS | a44b530f |
| 19 | 编辑器命令高亮漂移审计：发现 sma_anim/sma_ik/sma_variant 缺失（tag.invalid）；补全 3 命令 + check_test_coverage.py 增编辑器命令漂移守卫（负向验证通过）（G4 中段） | vitest 28/28, tsc 0, 覆盖检查 PASS(负向 FAIL 验证), ctest 绿, 耦合 PASS | a8db10c4 |
| 20 | lspCall Lua 桥代码生成测试（导出函数 + mock evalRaw）：方法调用/luaString 转义/终止符升级/多参 join/无参（G4 中段，20% 里程碑） | vitest 34/34, tsc 0, ctest 绿, 耦合 PASS | f36b5415 |
| 21 | jsdom 组件测试基建（G4 中段）：安装 jsdom/@testing-library + SceneTree 组件测试（空态/元素渲染/计数标题/点击跳转 reveal/挂载聚焦/无元素提示）——vi.mock 阻断 Monaco | vitest 40/40, tsc 0, ctest 绿, 耦合 PASS | 4176d232 |
| 22 | jsdom 组件测试推广（G4 中段）：StatusBar 7 例（offline/connected/scene/token/paused/品牌/组合态）+ ExplorerView 9 例（分组渲染/空态提示/大小写不敏感过滤/错误显示/脚本双击打开含 inspect 失败降级/图片占位打开/OPEN EDITORS 生命周期） | vitest 56/56, tsc 0, ctest 绿, 耦合 PASS | c06d8786 |
| 23 | jsdom 组件测试推广 II（G4 中段）：OutputPanel 4 例（空态/分级行/500 条上限/引擎不可达静默重试）+ DebugView 11 例（轮询镜像 store/暂停徽章/离线降级/Run 成功失败与禁用态/Stop/断点设置与非数字回退/清空/Continue） | vitest 71/71, tsc 0, ctest 绿, 耦合 PASS | af6a1995 |
| 24 | jsdom 组件测试推广 III（G4 中段）：ActivityBar 4 例（四视图按钮/图标/标题/active 类/点击切换）+ VisualView 12 例（帧渲染与错误/状态行/统计带单位与 tier/SMA 校验成功 meta+骨架树+动画/校验失败错误列表/evalRaw 加载与样本降级/本地 JSON 校验守卫/保存/Live2D 列表与加载/帧点击拾取）——SmaSkeletonCanvas vi.mock 桩 | vitest 87/87, tsc 0, ctest 绿, 耦合 PASS | 08e95f2b |
| 25 | jsdom 组件测试收官（G4 中段，25% 里程碑）：AiPanel 11 例（对话生成+aiwriter 桥/luaString 转义/AI 错误提示/eval 失败/无文档禁用守卫/尾部嵌入/场景规格守卫/生成插入/诊断行号解析/审查发现与通过/忙碌禁用）+ EditorArea 编辑器注册表 6 例（未注册 no-op/居中+定位+聚焦/行号钳制/多路径路由/注销/身份守卫）——monaco 全桩；vitest 破百 | vitest 104/104, tsc 0, ctest 绿, 耦合 PASS | 387c8349 |
| 26 | G4 功能增强：场景检查器（Inspector）——parseTagParams 全量参数表提取（引号/数字/裸旗标/=内值/未闭合容错）、SceneTree 点击联动 inspected 状态 + 高亮 + aria-pressed、新 InspectorView（类型/命令/全参数表/源行）、挂入 App explorer 侧栏 | vitest 117/117, tsc 0, vite build 0, ctest 绿, 耦合 PASS | 3a9feef8, ffc70167 |
| 27 | G4 功能增强 II：时间线视图——buildTimelineSections 纯函数按 *label 分段（序言/null 标签、空源、无标签脚本、空段）、类型过滤 chips（All/BG/FG/CH/Audio/Labels）、段/元素点击跳转+检查器联动、挂入 explorer 侧栏 | vitest 126/126, tsc 0, vite build 0, ctest 绿, 耦合 PASS | 0b6ae612 |
| 28 | G4 功能增强 III：引擎执行位置联动——RpcStateResult 新增 currentCmd（当前执行元素 [ch]/[bg]/text），main.cpp Lua 快照从 ctx.tokens[token_index] 提取，HTTP+stdio 三端点输出 current_cmd，编辑器 store engineCmd 镜像 + StatusBar cmd 行 + VisualView exec 行；headless RPC 冒烟全过 | vitest 128/128, tsc 0, C++ 711/711（+1 断言）, Lua 120+29, ctest 11/11, 耦合 PASS, headless 冒烟 PASS | 06846d67, a4f93628 |
| 29 | G4 收尾：时间线引擎执行状态条——engineConnected+engineCmd 时显示 ▶ 脉冲 + 当前命令 + token 位置，离线/空闲隐藏（复用 round 28 current_cmd 镜像） | vitest 131/131, tsc 0, vite build 0, ctest 绿, 耦合 PASS | 6c1be1a3 |
| 30 | G5 Web 导出三路径并行调研（30% 里程碑）：A emscripten 原生编译（条件性，6-10 周 Demo，渲染线程/COOP-COEP/Live2D 风险）、B 轻量 Web 播放器（wasmoon Lua5.4 + 90% 纯 Lua KAG 栈复用，1.5-2.5 人周，推荐首选）、C 移动/生态（SDL3 Android 模板就绪）；收敛决策 B 近期实施 + A 中期 PoC | 调研 3 报告 + 决策文档（725 行），门禁无代码变更 | 0578d6f5 |
| 31 | G5 实施第一步：wasmoon 验证 spike（3 连 PASS）——spike1 tokenizer+lpeg 原样运行、spike2 编译前端（schema/expr/compiler）55 tokens/3 labels、spike3 完整调度器执行 demo（55→53 token 推进）；**重大发现：Lua 5.5 通用 for 循环变量 const 化**——4 处 5.4 合法/5.5 崩溃代码修复（compiler/schema/ks_bake/scheduler）+ 4 例防回归测试（test_compiler 10a-10d，39/39） | vitest 131/131, Lua 120+29（+4 新断言）, C++ 711/711, ctest 11/11, 耦合 PASS | a14c2b44 |
| 32 | G5 MVP 核心原型：spike4 真实 kag 命令表 + JS 绑定适配层——9 命令模块加载、galgame_demo 55/55 tokens 零错误执行、good_end 结局解锁、20 次真实绑定调用（load_texture/audio_play/set_layer_image）；适配层契约实测（add_layer 返回节点表/Type 常量表/get 缺失返 nil） | spike 4/4 PASS，引擎门禁无变更 | 4f0fe235 |
| 33 | G5 Web 播放器 MVP（web/ 目录）：wasmoon bridge + AdapterCore 绑定状态机 + DOM 渲染器；帧驱动协程执行（[p] 点击暂停/autoClick/语音模拟）；galgame_demo 完整 DONE:53 + good_end 解锁 + 5 图层 + bgm/voice/se + 2049 绑定事件；12 vitest + Node 集成测试 | web 12/12, spike 4/4, 引擎门禁无变更 | 24324814 |
| 34 | G5 浏览器全流程验证：Layers.snapshot() 纯导出、bridge 收集 TextScene draws→core 覆盖层、render_text 参数契约修复、tag 前缀 img/div 分类、跨 run 纹理重映射；jsdom 集成测试（真实 wasmoon + DOM + galgame_demo：park→点击推进→DONE→hana bg+角色立绘渲染） | web vitest 13/13, Lua 120+29（+3 snapshot 断言）, 耦合 PASS | c57b8f15, c49e6742 |
| 35 | G5 导出链：ks_bake --web story bundle——bakeWeb（compiler.serialize 场景序列化 + storage/file 资产发现）、encode_lua_literal 导出、非 ASCII 路径 CWD 剥离、CLI --web 产出 cache/story/story.lua（4 场景 6 资产）；web 播放器零解析加载验证（.ksc + story bundle 均 load 直取） | web vitest 15/15, Lua 120+29+17, compiler 39, 耦合 PASS | e2764bd7, dc91cafa |
| 36 | G5 浏览器部署形态：vite 配置（publicDir 仓库根）静态服务 /scripts /demo /assets /cache/story 全部 200；main.mjs 优先 story bundle（runFromBundle 零解析）运行 + 点击推进；资源布局测试 3 例（web 18/18） | web vitest 18/18, vite 全资源 200, 引擎无变更 | b3500e69 |
| 37 | G5 音频真实化：AudioEngine（WebAudio 3 总线 bgm/se/voice、解码缓存、play/stop/isPlaying/音量），bridge 音频委托真实引擎 + core 状态降级；5 引擎测试（总线/生命周期/缓存/降级）；web 23/23 | web vitest 23/23, 引擎无变更 | 2cba6e3d |
| 38 | G5 文本排版对齐：bridge 收集 TextScene draws 为 Lua 表（x/y/rgb/scale/bold/italic）经 wasmoon 表桥接直达 JS，core.setDraws + DOM 渲染器逐条绝对定位 span；textBuffer 平面回退保留；flow 断言 span left/top/color/fontSize | web vitest 23/23, 引擎无变更 | 42628adb |
| 39 | G5 图层动画：CSS transition 补间（left/top/opacity 300ms）插值引擎驱动的 sprite_move/sprite_fade、opacity 0..255 归一化 0..1、canonical 节点 mutator（Lua proxy 引用解析）、main.mjs rAF 渲染循环；flow 断言 sprite_move 20+ 帧至 x=120 + transition 就位 | web vitest 23/23, 引擎无变更 | 0392b85f |
| 40 | G5 播放器 UI 收官（40% 里程碑）：backlog 历史（core 快照提交不同文本页 + 面板滚动回看）、自动推进开关（1.2s 定时点击）；3 backlog 测试；web 26/26 | web vitest 26/26, 引擎无变更 | 65c290d1 |
| 41 | G6 示例库起点：showcase.ks（25 命令全覆盖：文本/图层/音频/特效/流程 + random/if/jump 分支 + ending 解锁；56 tokens/6 labels）；backlog 历史机制设计（[p] 页快照） | 引擎 compile 通过, web showcase 全流程 PASS | c8ede0d6, 628d7b64 |
| 42 | G6 示例库收尾：backlog 完整实现（Lua 累积 __SCENE_BACKLOG + core.pushBacklog 快照去重不影响视图）+ showcase flow 测试（DONE + 9 页历史 + ending）+ 示例库文档 docs/guides/sample-library.md | web vitest 27/27, bundle 5 场景 | 628d7b64, c8ede0d6 |
| 43 | G6 教学示例库：demo/tutorial/ 6 个递进式教程（01 hello → 02 文本 → 03 图层 → 04 音频 → 05 分支 → 06 特效），全部过 ks_check 契约零警告 + ks_bake 编译 + Web 播放器 DONE 运行验证；Web 播放器新增 ending 解锁记录（bridge 导出 ctx.seen_endings → core.recordEndings 去重事件）；flow 测试固化教程全路径 + adapter 测试 2 例；sample-library.md 教程路径表；story bundle 11 场景 | web vitest 30/30, Lua 120/120+29, C++ 711/711, ctest 10/11（AI smoke 跳过）, 耦合 PASS | (round 43 提交) |
| 44 | G5 Web 播放器体验完善 + 生产构建：场景选择器动态化（scene-options.js 纯函数分组 demo/tutorial/other，7 例测试）、结局解锁面板（core.endings 同步）、音频状态行（bgm/se/voice 实时）；**vite build 生产构建打通**——移除 bridge.js 死代码 node:path 导入、assetsDir web-assets 规避与游戏资产目录冲突、copyRuntimeDirs 插件精选复制运行目录、es2022 顶层 await；resources 测试固化 dist 布局（5 例）；静态服务冒烟全 200 | web vitest 39/39, Lua 120/120+29, 耦合 PASS, 覆盖 PASS | 377771eb, 0825706d |
| 45 | G6 教程扩展 + Web 适配层补洞：tutorial_07（存档/读档：slot/save/load + tf.save_result/load_result 降级分支教学）、tutorial_08（系统 UI：unlock/gallery/music/history/chapter）；**Web 适配层修复 2 个真实缺口**——KAG 绑定表缺失（save_game/load_game/capture_thumbnail 桩，save/load 从崩溃变优雅降级）与系统 UI 模块桩缺 show/_hideAll（gallery/music/history/chapter 从 nil 崩溃变安全 no-op）；flow 测试扩到 8 教程；bundle 13 场景；sample-library 教程表 +2 | web vitest 39/39, Lua 120/120+29, C++ 711/711, ctest 10 通过+AI 跳过, 耦合/覆盖 PASS | (round 45 提交) |
| 46 | G5 Web 存档桥接 + **引擎级 set 缺陷修复**：1) Web 播放器真实存档——storage 后端（浏览器 localStorage/测试内存注入）、save_game 序列化、load_game 经纯 Lua KAG 包装表（wasmoon JS 代理返回值是 userdata 会破坏引擎 type 检查，Lua 字面量桥 + load() 解析绕开）、[load] 成功恢复变量+场景语义（stop_flag）；2) **引擎缺陷：`[set f.x = 5]` 的 "=" 占位符被存为值**（compiler normalize_params 剥离并移位，桌面+Web 双修）；3) `[set]` 不表达式求值——showcase/tutorial_05 的 `math.random` 分支改用 `[eval exp=]`（此前分支从未随机）；回归测试 11a-11e（44/44）；教程 07 重排（load 置末，双环境语义一致） | web vitest 40/40, Lua 120/120+29, compiler 44/44, C++ 711/711, ctest 10 通过+AI 跳过, 耦合/覆盖 PASS | (round 46 提交) |
| 47 | G5 Web 播放器 [load] 场景恢复（resume）：模板外层循环（result=='' 驱动）支持协程结束后重载存档场景——dead 分支检查 ctx._pendingLoadScene/_pendingLoadToken，__load_scene_tokens 按需解析（裸名 + demo/ 前缀双向查找，runScene 用 tokenizer、runFromBundle 用 compiler.deserialize）；修复 save_game 未规范化 scene_path（_safeScenePath 拒绝裸名导致 [load] 永不设 pending）、pcall 解包错（nt 是布尔）、外层重建 co 误用初始 tokens 变量导致死循环（改用 ctx.tokens/ctx.token_index）；resume 测试：存档点续玩 LINE-A3/A4 进 backlog | web vitest 41/41（+resume 用例）, Lua 120/120+29, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 47-48 提交) |
| 49 | G5 Web 播放器存档管理 UI 闭环：bridge 新增 listSlots（storage 枚举 0..99 + scene/token/savedAt 元数据）/deleteSlot（幂等）/saveCurrent（驱动引擎 SaveCommands.save 用最后 run 的 ctx，__LAST_CTX 模板导出）/loadSlot（内置 [load] 小场景 + resume 续玩）；index.html+main.mjs 存档面板（槽位列表/保存/读档/删除/刷新）；flow 测试 2 例（槽位管理、UI 读档续玩） | web vitest 43/43, Lua 120/120+29, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 49 提交) |
| 50 | **50% 里程碑** G9 脚本深度前哨：审计发现 3 个真实缺陷——1) `${expr}` 插值不翻译 TJS 运算符（`${f.hp > 20 ? 'high' : 'low'}` 原样保留，`load()` 拒绝 `?:`）→ schema 插值前调 expr.translate；2) `[set f.name = "Aoi"]` 存含引号值（tokenizer 无 dotted-key 分支）→ param 语法支持 ident(.ident)* = value，compiler/ks_check 同步 dotted→positional 展开；3) 引号值在裸参数下不剥离 → qval 路径正确剥离；回归测试 12a-12e（compiler 49/49）；教程 09（插值教学，Web 播放器全验证）；bundle 14 场景 | web vitest 43/43, Lua 120/120+29, compiler 49/49, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 50 提交) |
| 51 | G9 脚本深度（Phase D 起航）：**契约↔处理器完整性审计**——84 契约命令全部有 handler（✓）；52 个无契约 handler 分类（内部/别名/流命令 vs 真实命令），为 18 个真实命令补 schema.define（unlock/rollback/saveload/playvoice/voice/stopvoice/waitbgm/waitsound/waitclick/blur/fade/layfade/stopvideo/skip/button/endbutton/endselect/select），契约表 84→102；剩余 32 个确认全部为别名（g/ct/clear/se/sel/showtext/clearscreen/br/hr/l/ld/r/s/er/endtag/endform）、内部（_ 前缀、wait_click、vfx、push_backlog、资源缓存族）或流命令（call/jump/macro/end）——无需契约；command-contracts.md 重新生成（102 命令）；编辑器 KAG_COMMANDS 补 5 个缺失（saveload/stopvoice/blur/fade/layfade），覆盖守卫 PASS；schema_doc 文档同步 | web 43/43, Lua 120/120+29, compiler 49/49, C++ 711/711, ctest 10+AI 跳过, 编辑器 tsc PASS, 耦合/覆盖 PASS | (round 51 提交) |
| 52 | G9 契约运行时测试 + **孤儿套件假绿修复**：新增 test_contracts_runtime.lua（18 断言覆盖 round-51 契约命令的真实 handler 执行，headless 安全验证）；修复 3 个无后端崩溃（blur RTT alloc 守卫、saveload 菜单 pcall require + show 守卫、listsaves 绑定守卫）；**重大发现：run_orphan_tests.lua 的 test_kag_core 自行 os.exit(0) 终止进程，其后的 test_kag_e2e/system_flow/macro/p2_features 4 个测试从未执行（round 13 声称 8 个全过实为 4 个）**——runner 改为每个测试子进程隔离（io.popen），修复 Windows popen 引号坑（可执行带引号失败、反斜杠、arg[-1] 相对路径），孤儿套件 9/9 真实全绿（0.5s） | web 43/43, Lua 120/120+29, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 52 提交) |

| 53 | G9 表达式语言 **?? 空合并运算符**：文档提到但从未翻译的真实缺口——expr.lua translate_operators 新增 ?? → or（Lua or 返回第一个真值：nil/false 回退，0/"" 存活，对数字/字符串变量语义精确，字符串字面量内不翻译）；test_expr_lang +5 断言（缺失回退/0 保留/现值保留/嵌套算术/字面量不翻译）；集成验证 [if exp="f.missing ?? 0 > 0"] 分支正确 + ${f.hp ?? 99} 插值保留 0；kag-expression-language.md 运算符表 +?? 行；**孤儿套件 git-bash 兼容修复**——cmd.exe 对未加引号的 ( ) 路径分词截断（'D:\...\Caesura' not recognized），io.popen 对含空格/圆括号的绝对路径（git bash 下 arg[-1] 形态）用 call "..." 包裹，9/9 真实全绿 | web 43/43, Lua 120/120+29, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 53 提交) |

| 54 | G9 插值平衡花括号 + 契约运行时测试扩展：**${expr} 嵌套花括号修复**——旧 `%${([^{}]+)}` 对 `${ {a=1,b=2}.b }` 截断泄漏原文；新平衡扫描器（quote 感知 match_brace，未闭合 ${"{ 保留原文）；**发现 Lua 语言限制：表构造器不能直接索引（{1,2}[1] 语法错误，构造器非 prefixexp）**——求值前给前导构造器加括号 ({a=1}).a；test_schema +6 断言（构造器/引号内花括号/未闭合/空 span/算术）、test_compiler 12f；test_contracts_runtime +20 断言（voice/stopvoice/waitbgm/waitsound/waitclick/skip/unlock/button/select/rollback/stopvideo/fade/layfade）→ 38 断言全绿 | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 54 提交) |

| 55 | G9 [switch] 表达式支持：EXPR_TOKENS["switch"]="exp"（原注释 'switch expr is a variable name'——kag-commands.md 声称 expr 支持但从未实现，文档/代码出入修复）；compiler exprs 侧表纳入 switch（AOT bytecode dump 自动覆盖）；scheduler switch 分支新增 exp= 求值（eval_expr_translated + tostring 等值比较，与 [if] 同机制；表达式失败/nil 时回退原文走 default）；裸 positional 形式完全兼容（hand-built/编译流双路径验证）；test_switch +9 断言（变量等价/数值 tostring/布尔表达式/&& 翻译/缺失变量回退/.ks e2e）16 全过；文档同步（kag-commands.md exp 行、kag-expression-language.md 引言） | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 55 提交) |

| 56 | G6/G9 循环教程 + Web 播放器端到端验证：**tutorial_10_loops.ks**（[for] 正序/倒序 step=-1、[while]+[eval] 递减、每场景 65536 迭代守卫说明；循环体内 [ch]+[p] 逐轮点击推进）——ks_check 零警告 + ks_bake 编译 + wasmoon 播放器 DONE 全流程（[for]×3/[for] 倒序×3/[while]×3 逐行验证）；flow 测试教程列表 01-08 → 01-10（补 09 插值教程缺口）；story bundle 14 → 15 场景；sample-library +row 10 | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 56 提交) |

| 57 | G9 编辑器 LSP 契约校验测试：headless_rpc_smoke.py +11 断言（18→30 全过）——hover 对 round-51 契约命令经引擎 eval 桥接 kag.lsp.json 返回契约 JSON（blur duration type=number、fade、unlock、saveload mode、select params 列表）；completion 前缀补全（[sav→saveload、[bl→blur、[ch →text 参数）；diagnostics 校验表达式编译错误（[if exp="f.hp >"]）、_require_any（[playbgm] 缺 file/storage）、未知命令警告；**发现并记录：diagnostics 不做参数类型校验**（类型是运行时 coerce/clamp，属设计行为） | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过（含 LSP 冒烟 30 检查）, 耦合/覆盖 PASS | (round 57 提交) |

| 58 | G6/G9 教程收官：**tutorial_11_switch.ks**（裸变量选择器 KAG3 兼容、exp= 数值匹配、布尔条件 + TJS &&、缺失变量→default；case tostring 等值无 fallthrough）——ks_check 零警告 + ks_bake 16 场景 + wasmoon DONE 全流程；flow 测试教程路径 01-10 → **01-11**；sample-library +row 11；子代理调研：**[until] 无每场景迭代守卫**（while/for 有 65536 护栏，until 仅靠 timeout——8i-8o 测试方案待 round 59）、push 准备（origin 领先 125 提交、CI 全任务可解析但 **run_lua_tests/orphan 未进 CI**，仅本地跑） | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 58 提交) |

| 59 | G9 until/button 边界 + **[until] 迭代守卫加固**：scheduler until 新增每场景帧数守卫（与 while/for 同款 WHILE_MAX_ITERS=65536，实测 65538 帧触发 error——此前 until 仅靠 timeout，大 timeout + 永不真条件会无限轮询）；test_scheduler 8i-8o（timeout=0 立即穿透/负值钳 0/默认 60s 不早退/stop_flag 中止场景/operation cancel 结束等待/不触碰 waiting_input/非布尔真值/1 帧 timeout）；test_choice C1-C6（数值不等式过滤/缺失变量假值溶解/空串无条件/组合 TJS 条件/endselect 对齐/空选择 no-op）——test_scheduler 34 全过、test_choice 全过 | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 59 提交) |

| 60 | **60% 里程碑：统一 push + 三平台 CI 全绿**——ci.yml 补 Lua 套件步骤（Windows vendored lua / macOS brew lua / Linux apt lua5.4，此前 CI 只跑 ctest、Lua 全靠本地）+ Linux 补 check_test_coverage 对齐；**push 136 提交**（9ff69e0c..cb91f058）到 origin；CI 修复 4 轮：①main 套件 3 测试跨平台化（carc_import：bin/Debug 与 bin 二进制探测 + portable reset_dir/重定向 + ls -1 替代 dir /b；ks_bake：ensure tmp/cache/ksc；mods：portable mkdirs/cleanup）②孤儿 runner to_native 路径分隔符（POSIX 上反斜杠是字面文件名）③test_integration carc_pack 候选按平台（无 .exe、单配置 bin/）④src/main.cpp 规范大小写（Windows 大小写不敏感掩盖）；最终 5 job 全绿 | 本地门禁全绿 + CI 5/5 job PASS（Windows D+R / macOS / Linux / Package） | (round 60 提交) |

| 61 | G9 表达式翻译补洞：①**三元在 [ ] 索引内不翻译**（find_top/match_colon 把 [ ] 当嵌套深度，f.arr[flag ? 1 : 2] 的原样 ? 使 Lua load 失败、[if] 静默走 else）——新增 translate_brackets 从内向外预翻译括号内容，外层三元配对不受影响；test_expr_lang +6（then/else/算术组合/外层三元包索引三元/字符串值/translate 无 ?）②**[eval] 缺 TJS 运算符翻译**（EXPR_TOKENS 无 eval，[eval exp="x = a && b"] 是非法 Lua）——运行时 translateOperators（仅运算符不翻译三元：赋值语句里 (a and b or c) wrap 会破坏 x = 前缀）+ expr.translateOperators 导出；test_compiler 11f-11h（&& / || / ! 赋值）53 全过 | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 61 提交) |

| 62 | G9 插值长括号支持：match_brace 识别 Lua 长括号 ${[=[...]=]}（任意层级 [[、[=[、[==[）——长字符串里的 } 不再提前闭合 ${...} 跨度（此前 ${ [[}}]] .. f.x } 截断泄漏）；构造器括号化共用同一函数自动受益；test_schema +3（[[}}]] 值/level-1 [=[}]=]/纯表达式回归） | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 62 提交) |

| 63 | G9 同名嵌套 [for] 死循环修复：_forStackMarks 从布尔改为**栈计数**（entry 0→1 初始化计数、嵌套 +1、endfor 完成/break 递减、rewind 重入经 _forRewound 标记不重复递增）——此前内层 endfor 把外层 mark 清空，外层重入时计数被重新初始化永不终止；同名共享计数器语义下外层只跑 1 次（IN,IN,OUT）而非死循环；test_scheduler 10（终止/序列/最终计数 4）37 全过 | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 63 提交) |

| 64 | G9 审计收官：**lpeg 排除集转义修复**——gsub("(%W)","%%%1") 把 \n 变成模式类里的 %n（**全部控制字符类的超集**，非换行符），S(" \t\r\n") 会把任意控制字节挡出文本流；改为仅转义模式特殊字符（^ $ ( ) % . [ ] * + - ?），空白/控制字节原样进类；探针验证控制字节保留、空格/星号/方括号/换行照常截断；test_tokenizer +1（text run 保留 \x01）；**G9 审计 5/5 全部完成**（三元索引/eval 运算符/长括号/同名嵌套 for/lpeg） | web 43/43, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 64 提交) |

| 65 | Web 播放器表达式 parity：flow 测试新增 expr-edge 用例（44/44）——三元索引 then/else（${f.arr[f.flag ? 1 : 2]}）、?? 空合并回退、长括号插值（[[x}}]]）、[eval] && 运算符、[switch exp=] 数值匹配、[for] 表达式边界——round 53-63 桌面修复在 wasmoon 下行为完全一致 | web 44/44, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 65 提交) |

| 66 | 性能基线（热路径规模化确定性测试）：test_schema 500-span 插值（首尾跨度正确 + 计数 500 + <5s）；test_expr_lang 2000x 缓存求值（<5s）+ 200 项数值链（=20130）+ 运算符链；test_tokenizer 2000 命令场景（=2002 token，<10s）——为插值扫描器/表达式缓存/tokenizer 文本流设回归护栏 | web 44/44, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 66 提交) |

| 67 | 文档一致性核对 + CI 保鲜守卫：api-stats.md 普查过期（契约 84→102、C++ 用例 638→711/断言 6160→6656、绑定函数 157→149、UnifiedBinding 移除）——重生成提交；command-contracts.md 验证新鲜（102 命令）；ci.yml Windows job 新增「Generated docs freshness」步骤（重生成 + git diff --exit-code 失败即红） | web 44/44, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 67 提交) |

| 68 | 表达式深化：①**三元在括号内翻译**（find_top 只匹配 paren 深度 0——(f.flag ? a : b) 从未被翻译；新增 translate_parens 与 brackets 同构的内外翻转压平）②**eval 三元赋值**（translateAssignment：首个顶层 '=' 拆 LHS，RHS 全管道翻译——x = cond ? a : b 合法；修 for 循环内改 i 无效的 Lua 陷阱改 while、== 双等号跳过、LHS/RHS trim；ks_check 与 lsp diagnostics 同步兜底）③tutorial_12_expr_combo（三元索引/?? + switch exp/循环插值/eval 综合）flow 01-12、bundle 17 场景 | web 44/44, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 68 提交) |

| 86 | **契约元数据补齐 + storage 上限对称修复 + i18n 边界 + VisualView 层快照 + scene-options 澄清（6 子代理全收敛）**：①**契约 _meta 补齐**——11 个 KAG3-compat 命令（bgm/play/playstop/ld/cancel/close/saveplace/loadplace/listsaves/voice_wait/waitforclick）在 kag.lua 注册补 _meta {category,blocking,desc}（audio/layer/system/save/text 分类、voice_wait/waitforclick blocking=true）；契约文档重生成后 118 命令 100% 有 _Category 头行、总数不变、覆盖守卫 OK ②**storage 大小上限对称修复**——round 85 记录的不对称缺陷：SaveManager::writeFile 无 MAX_SAVE_SIZE 守卫（>10MiB save 返回 true 落盘但 load 静默 null）；修复双层守卫：payload 预检（>10MiB 拒绝不落盘+DEBUG_ERR）+ on-disk 字节兜底（加密 +32 头后可能超限）；测试断言翻转为对称行为（save false/磁盘无文件），Storage 39/39 ③**live2d-setup carc:// 清零**——唯一残留引用改纯路径 storage=live2d/...（与资产目录一致）；全仓库 carc:// 仅剩说明性文字（carc-packaging.md 明确写"无此协议"、ROADMAP 历史记录），实际使用零残留 ④**i18n 命令边界 +26**——test_i18n 118→144：语言切换运行时（settingsValues.language/i18n.current/字典激活/relocalize 接线）、复数边缘（负/非整数/nil/字符串落 other）、{n} 与普通占位符混用、serialize_field %q 转义往返（引号/反斜杠/换行）、回退链组合（translate≡localize/缺键→默认语）、命令参数边界（无 language 降级不抛/schema coerce 拦截/重复切换幂等）；零实现 bug；记录：i18n 命令语言切换会重建 i18n.lines（运行时注入翻译被清空，符合设计）⑤**scene-options 澄清 +17**——实际模块是纯无状态场景分组助手（classifyScene/buildSceneOptions），非播放器设置；按真实职责深化（分类法/输入校验/默认空组/纯净性），web 117→134；建议：播放器设置模块（分辨率/语言/auto）不存在，如需新建需接线 createPlayer ⑤**VisualView 层快照 +17**——新 layerSnapshot.ts 纯函数（buildLayerSnapshotSnippet/parseLayerSnapshot/layerSlot）+ 层树实时渲染（/api/eval 读 layers.forEach，槽位分组+z 排序+纹理句柄+透明度）、点击层联动 Inspector、hover 高亮、缩放平移钳制（clampPan/clampZoom/ResizeObserver）、引擎连接 live 徽标+轮询 1500ms/断开静态降级；editor 305→322，tsc 干净 | web 134/134, editor 322/322, Lua 124/124, 孤儿 17/17, C++ 805/805, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 86 提交) |
| 85 | **web 音频引擎深度 + storage 边界 + call/return 调用栈 + TimelineView 联动 + 契约一致性审计 + 文档修复（6 子代理全收敛）**：①**web 音频引擎 +27**——总线路由（源连 gain 非 destination/三总线并发/同名并发切旧源/总线静音隔离）、音量（逐总线独立/0/1/中间/全局静音/持久/play 覆盖）、生命周期（suspend/resume/destroy 后全降级/double destroy/重建三总线）、资源容错（fetch reject 降级 false/404/失败不缓存可重试/decode reject/空 buffer/无源 stop no-op）、状态（isPlaying 按 kind/onended 自动清活动表/迟到 onended 不误清新源）、桥接契约（方法面+调用形态）；**修 1 真实 bug：_load 失败把 rejected promise 泄漏给 play 抛异常（catch 返回 null+删缓存）**；setBusVolume NaN 兜底、play volume:0 静音修正；web 90→117 ②**storage 边界 +8**——1MiB 大 payload 双路径往返、>10MiB 上限不对称锁定（save 可写 load 拒）、UTF-8 CJK/emoji/组合字符/NUL 往返、未知未来版本 v99 透传、v2→v5 中间版本步进迁移、10 槽快速连续 save+load 顺序一致（无内存缓存）、自定义 InMemorySaveProvider 全流程、provider 错误优雅传播；C++ 797→805；**记录：save/load 大小上限不对称（writeFile 无 10MiB 守卫 readFile 有）、自定义 provider 绕过 AES 加密（设计观察）** ③**call/return 深度 +42**——新 test_flow_edge_call.lua（登记主套件，Lua 123→124）：3 层嵌套 LIFO+lf 逐帧隔离、20 层深不崩、子程序 lf 改写隔离（f.* 跨帧共享）、裸 return 终止/跨场景 call 还原/宏体内 return、for 每迭代 call、switch 内 return 不泄漏（round 83 jump 栈重置语义）、call×saveplace（栈不序列化、load 后 return 变裸 return）；零实现 bug ④**TimelineView +21**——引擎位置联动（timelineTokenToLine 纯函数 + sceneMatchesDoc + outline-current 高亮）、键盘导航（↑↓/Home/End/Enter+tabIndex）、选中态同步（aria-pressed+inspected）、Text 过滤 chip、空脚本占位；决策：不扩展共享 SceneElement（低爆炸半径），内部新增 collectTextLines/buildTimelineSectionRows；editor 284→305，tsc 干净 ⑤**契约一致性审计**——schema 118 契约 vs KNOWN_COMMANDS 160（覆盖全部 118+41 流程词）vs lsp 索引 179 全覆盖 vs 高亮同源，全部一致零修改；KNOWN_PARAM_HINTS 24 命令逐参数核对一致；契约文档新鲜零 diff；**发现：11 个契约命令（bgm/cancel/close/ld/listsaves/loadplace/play/playstop/saveplace/voice_wait/waitforclick）在 kag.lua 注册缺 _meta 元数据（文档无 category/blocking 头行）——记录待补** ⑥**carc/asset-pipeline 文档修复**——核实全部过期点属实：工具路径 bin/Debug/carc_pack.exe、list/extract --path 子命令、无 carc:// 协议（纯路径+三归档自动注册）、DeltaCARC 已实现未接入组合根、FFmpeg 全格式 vs pl_mpeg 回退；另发现 asset-pipeline 用不存在的 KAG.preload() 绑定（实际是 [preload] 标签）已改；**遗留：live2d-setup.md:97 残留 carc:// 引用待修** | web 117/117, editor 305/305, Lua 124/124, 孤儿 17/17, C++ 805/805, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 85 提交) |
| 84 | **C2 宏展开死循环修复 + G3 SDK 桩 + 表达式实参边界 + DebugView 深度 + web i18n×advance + 指南审计（6 子代理全收敛 + 主代理 expr 修复）**：①**C2 宏体内 goto 重展开死循环修复**——根因比预想深：静态安全宏被 compiler 编译期内联（运行时无 [m1] 调用、_macroStack 恒空、round-75 深度守卫永不触发），内联后宏体 goto 反向跳回重入无限循环；修复：scheduler 新增 **反向跳边记忆守卫**（_backJumps 按 origin>target 键，同一反向边只允许首次，重复触发 WARN+fall-through 切断回绕；run 入口重置、跨场景重计数）；同时覆盖编译期内联与运行时 splice 两种形态；C2 断言从锁定缺陷（n>=300）翻转为有界终止（<300、序列 MSTART,AFTERFIRST,MSTART,MSKIP,AFTERSECOND、宏栈=0），test_flow_edge 28→29 ②**G3 SDK 条件编译测试桩**——发现核心缺口：SteamBackend.cpp 无条件编译、#else 降级分支在无 SDK 构建真实执行却从未被测试；test_steam +6（真实类非桩：init→false/name=Steam/achievements/stats/cloud 全降级/runCallbacks/幂等 shutdown）+ SDK 符号静态守卫；test_live2d +3（#ifdef 包裹/SDK include 隔离/组合根双分支）；G3-sdk-stub-notes.md 文档（集成方式/降级行为表/覆盖矩阵/带 SDK 路线图）；C++ 788→797 ③**表达式实参边界 +55**——test_expr_lang2.lua（登记主套件，Lua 122→123）：优先级链/字符串表函数调用/数字边界/null 链/作用域/嵌套三元；**主代理修复 translate_parens 多参调用三元吞逗号 bug**——括号内容整体翻译使 f.calc(f.flag ? 1 : 2, 3) 的 3 并入 else 分支：顶层逗号分段独立翻译 + 每段 trim 再 join；expr2 三条 LIMIT 锁定断言翻转为修复后行为 ④**DebugView 深度 +14**——断点列表排序去重/单条移除/Clear All 门控/断开降级提示、inspect 面板（类型标签渲染/请求中禁用/错误行）、frame capture（png+尺寸/空帧占位/失败降级）、会话状态机（Continue 仅 paused 可点/断开全禁用）；editor 270→284，tsc 干净 ⑤**web i18n×advance 组合 +5/46 断言**——i18n 切换后 advance 新页新语言、语言跨 advance 持久（i18n.current 跨场景）、i18n+save/load 语言保持（save.lua 持久 state.language）、backlog 重本地化×advance、复数×advance（en one/other、zh 恒 other）；零桥改动（__CTXREF 天然正确）；web 85→90 ⑥**指南审计**——kag3-import.md 7 处修正（契约 78→107 动态说明、goto→jump 行、宏参数 &N→%N%、PARAM_ALIASES、CONFLICT_NOTES、monocro 建议改 palette/flash、断言 41→93 实测）+ getting-started.md（FFmpeg 回退/Windows lua 路径/新增 Smoke Checklist 实测可行）；报告 carc-packaging.md 与 asset-pipeline.md 过期点（待后续轮） | web 90/90, editor 284/284, Lua 123/123, 孤儿 17/17, C++ 797/797, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 84 提交) |
| 83 | **galgame flake H1 修复 + LSP 别名提示 + goto 流程深度 + web 存档 UI + 文档刷新（6 子代理全收敛 + 主代理 scheduler 修复）**：①**flake H1 修复**——LuaManager::loadScript 对 LUA_ERRFILE 加 4ms 一次性防御重试（Windows 瞬时文件打开失败=AV/IO 背压；语法/运行期错误码绝不重试；返回契约不变）；test_galgame_startup 三个 loadScript 断言改 loadOrCapture 诊断包装（失败可定位模块）；test_lua_manager +2 边界（成功不重试/语法错不重试/持久 ERRFILE 仍失败），C++ 786→788 ②**LSP KAG3 参数别名提示**——lsp.lua 新增 KAG3_PARAM_ALIASES（镜像 kag3_import：add/sub/mul/div/mod/dec var→name、csp/csl left/top→x/y）+ kag3_alias_target；unknown-param 诊断（severity=2）命中别名时 message 追加「（KAG3 别名：建议用 x= 代替 left=）」；纯提示不改契约/校验，协议兼容；test_lsp +10（csp left=/csl top=/add var= 提示、规范参数无提示、按命令限定、lsp.json 桥接序列化）③**goto/jump 流程边界 +28**——新增 test_flow_edge.lua（登记主套件，Lua 121→122）：goto×循环栈 9（for/while 头部回绕、switch case 无泄漏、跳出泄漏窗口）、call/return×goto 6（子程序内 goto→return 干净、深 call 栈、悬空帧不崩）、宏×goto 3、label 解析 5（EOF label/特殊字符/空白 target 拦截/重复 first-wins）；**主代理裁决并修复 A4/A5 循环栈泄漏缺陷**——intra-scene jump 处清空 _forStack/_whileStack/_ifStack/_switchStack/_forStackMarks（任意跳转离开循环体后陈旧条目泄漏污染后续同名 for：实测 11 次迭代应 2 次）；测试断言同步翻转为修复后（栈=0、同名 for 重新初始化、f.i=end+1 符合 Lua 语义）；**C2 宏体内 goto 无限重展开死循环记录待修**（goto 指向宏调用点后 label → MSTART,AFTERFIRST 无限重复，_macroStack 深度守卫不触发；测试帧上限保护不挂死套件）④**web 存档/读档 UI 边界 +6 用例/49 断言**——新 slots.boundary.test.js：slot 生命周期（save 后列表/删除幂等）、save 在 [p] 停点（load 不重放前页）、多 slot 隔离（A/B 场景各自恢复变量）、load 后 advance 组合（__CTXREF 交接到恢复场景）、损坏存档容错（非法 JSON/错结构优雅返回）、storage 可注入（内存 Map + localStorage 零写入断言）；web 79→85；load 后单次 advance 跑至 DONE 为引擎 [load] 恢复设计（记录不修）⑤**kag-language-tour 刷新 +122 行**——契约数 78→118、goto=jump 别名、宏深度语义（嵌套调用/定义/静态安全内联条件）、复数形态 G9、round 71-82 新命令速查 §19、表达式运算符全表 §20；sample-library 修正（showcase 25→26 命令、tutorial_09 误列 interpolate）；全部示例实测 ⑥**能力矩阵刷新**——D1 行补 G4-3/4-4/Inspector+commandLint（editor 210→270）、P2 基线（C++ 786→788、Lua 121+17 孤儿、web 76→85）；api-stats 重生成零 diff（接口 31/方法 278/契约 118）、cpp-interfaces 核对一致 | web 85/85, editor 270/270, Lua 122/122, 孤儿 17/17, C++ 788/788, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 83 提交) |
| 82 | **Web advance parity 收尾 + G14 发布自动化 + Inspector 深度 + 契约测试扩展 + 性能基线 + flake 根因（6 子代理全收敛）**：①**web advance/skip/auto parity**——runScene/runFromBundle 双路径实现 advance 语义：opts.advance+advanceScene 匹配时从持久化 __CTXREF/__CO（wasmoon 同实例 Lua 表/协程引用存活）恢复上次停点协程，waiting_input=false+__CLICK=true 驱动经过一个 [p] 只推进一页（镜像 kag_runner.on_click）；dead 协程经 wrap_co 从 ctx 位置重建；[load]/label jump 重包装同步写回持久化游标；createPlayer 未接线的 _co/_ctx 字段接线；main.mjs advance() 不再 click+全场景重跑；flow 42→45（基本推进/advanceScene 不匹配从头/变量跨页保持）②**G14 发布自动化**——scripts/gen_changelog.py（纯 stdlib 从 conventional commits 生成 CHANGELOG，--count/--since/--from-tag/--dry-run；修裸日期时区 0 条坑）+ CHANGELOG.md（round 70-81 手工润色分组版）+ docs/guides/release-process.md（构建→门禁→changelog→CPack→ZIP 验证→gh release）；CMakeLists CPack 段：确定性文件名 CaesuraAmeKAG-<ver>-<sys>-<proc> + SHA256 sidecar + LICENSE 嵌入 + SDL3 收窄 WIN32（修非 Windows 打 libSDL3.so/.dylib 进 ZIP）③**InspectorView 深度**——引擎实时状态条（场景名/token/暂停徽章，复用 useEnginePosition）+ 命令参数 lint（新纯模块 commandLint.ts：KNOWN_COMMANDS 单一事实来源、kagLanguage.ts re-export 保 API、~20 高频命令参数提示 unlisted/flag 软提示）+ label 双向跳转（jump engine→label + follow engine position）；editor 245→270（+25），tsc 干净；**主代理修 check_test_coverage.py 解析 commandLint.ts**（KAG_COMMANDS 变 re-export 后旧正则失配，改双源探测 + 宽松模式）④**契约运行时测试 +56**——test_contracts_runtime2.lua 8 类（数学链/除零、textspeed/cps 边界、vibrate/palette headless 降级、csp/csd/csl 路径、音频降级、notify toast、wait/delay、sel/button 选择）登记孤儿套件（16→17）；**主代理裁决修复 palette.load 缺守卫 bug**（set_night_mode→palette.load 直访 backend 全局 headless 崩，加 lut_available 守卫；探针验证 headless 降级不崩）；csp/csl left/top 别名确认为 kag3_import 导入期职责（运行时拒绝符合契约，记录不修）⑤**性能基线刷新**——perf 文档 26→84 行：round 66-81 零回归（schema 500-span 0.010s/expr 2000x 0.062s/tokenizer 2000 0.186s/frame_bench 274.6us 45% 占用），全部预算余量 >97% 无需优化 ⑥**galgame flake 根因 H1**——最强假设：Windows 瞬时文件打开失败→LUA_ERRFILE→loadScript 无重试返回 false（init.lua require 链约 30 文件）；已证伪 BackendRegistry 残留/JobSystem 线程/settings 污染/文件写入；建议：loadScript LUA_ERRFILE 一次性重试 + 测试 CAPTURE require 名 + CWD 硬化；NOTES 落盘 ROUND81-GALGAME-FLAKE-NOTES.md | web 79/79, editor 270/270, Lua 121/121, 孤儿 17/17, C++ 786/786, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 82 提交) |
| 81 | **Job 异常隔离 + G4-4 实机位置交叉引用 + Engine 生命周期 + 文档同步（6 子代理，5 收敛）**：①**Job 异常隔离缺陷修复**——round 80 记录的两缺陷之一落地：真实 JobSystem workerLoop 与 pollMainThreadJobs 的 work()/onComplete() 全部 try/catch（std::exception 报告 + ... 吞掉），NullJobSystem 同步镜像同语义；测试翻转 3 个旧"异常逃逸"断言为隔离断言 + 新增真实系统 worker/主线程隔离 + 队列继续排空（779→786，7198 断言）②**G4-4 引擎位置交叉引用**——useEnginePosition hook 以 1.5s 间隔经 /api/eval 探 _CAESURA_CTX（no-ctx/no-scene/{scene,token} 自描述永不上抛）+ enginePosition.ts 纯函数（token→outline 行映射、sceneMatchesDoc 全路径/裸名匹配）+ SceneOutline 行高亮 outline-current + 面板 ▶ 场景名；主代理补 11 个纯函数单测（editor 234→245）+ SceneOutlinePanel 测试隔离 mock 轮询 hook ③**Engine 生命周期 +5**——clean construct+init+shutdown 往返/destroy 后安全/destroy 未显式 shutdown 安全/EngineConfig 默认最小配置/显式后端覆盖透传 registry（test_engine_lifecycle +105 行，已注册 CMakeLists）④**G13 文档同步**——lua-modules.md i18n 复数 API（plural_category/_plural_form/translate 复数表格契约）+ editor-api-reference.md 附录 C LSP 语言服务方法（/api/eval 桥接）⑤**SA-B galgame flake 根因调查**——6 轮全量复跑 779/779 全绿未能复现；审计确认每 Engine 独立 lua_State（package.loaded 污染不可能跨测试穿越）、BackendRegistry 单例为唯一跨实例共享态；**结论：flake 非 package.loaded 污染机制，疑似与测试执行序/进程环境相关，记录待续**⑥**SA-E web advance 步进**——__ADVANCE/__ADVANCE_SCENE globals 声明落盘但主循环消费逻辑未完成（仅声明为死代码），主代理回退该不完整改动，round 82 重派 | web 76/76, editor 245/245, Lua 121/121, 孤儿 16/16, C++ 786/786, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 81 提交) |
| 80 | **80% 里程碑**：LSP 重命名 + G4 实机跳转 + i18n 复数 + 引擎边界补全（6 子代理）：①**LSP label 重命名**——lsp.rename(text,line,char,newName) 返回编辑集 {kind,line,col,length,newText}：定义点 + [jump]/[call]/[link]/[goto]/[sel]/[select] 引用（引号/裸名/位置槽全形态），locate_label_byte 从 token 源片恢复字节偏移；新名校验（非法名返回 nil）、未知标签空编辑；lsp.json rename 协议 {renamed,edits}（test_lsp 91→101）②**G4-3 大纲驱动实机跳转**——经 /api/eval 发 Lua 片段：_G._CAESURA_CTX 锚点（沙箱白名单）+ flow.find_label 校验 + kag.jump 设 _next_index 让运行中调度器接管——**零引擎改动**，实机验证 *right 跳转 ok（_next_index=36）；editor engineJump.ts 纯函数 + SceneOutline ▶ 按钮/右键 + Panel 守卫降级，editor 210→234，tsc 干净 ③**i18n 复数 + 数字格式**——词典顶层值可为 CLDR 复数表 {one=,other=}（en 1→one 否则 other；zh/ja 恒 other），translate(text,{n=}) 先按 plural_category 选形再插值 {n}；t() 防表泄漏回退 generic；ks_i18n --update serialize_field 往返保留表值（test_i18n 96→118）+ i18n.md 文档 ④**Job 系统边界 +10**——Null 同步完成/FIFO/异常逃逸（无隔离）/嵌套 fan-out；真实 JobSystem 免 GPU 构造、workerCount 匹配 computeWorkerCount（hw=0→4/hw<4→1/否则 hw-1）、waitIdle 排空（769→779）；**缺陷记录：IJobSystem 无取消 API、真实 workerLoop 无 try/catch 异常会终止进程** ⑤**HTTP smoke +13**——/api/eval 桥接同一 Lua 派发：math 链读回/契约≥118/stats 健康/存档槽往返/notify 降级/i18n 缺参（33→46）⑥**G13 终检**——被删模块零残留（仅 ROADMAP 历史行）、能力矩阵 4 行刷新（C3 加密/P5 预算/D1 RPC/P2 套件数）、api-stats 重生成（71 模块） | web 76/76, editor 234/234, Lua 121/121, 孤儿 16/16, C++ 779/779, ctest 10+AI 跳过, 耦合/覆盖 PASS, RPC 45/45 + HTTP 46/46 | (round 80 提交) |
| 79 | G12 死代码清理 + G8 预算 + 存储加密 + 教程回归 + G4-2（6 子代理）：①**G12 Lua 死代码审计清理**——引用图谱（C++ 入口只加载 init.lua+sandbox.lua）：删 4 文件（conductor.lua 调不存在的 scheduler.load_script 运行即错、parser.lua 遗留 tokenizer 适配、debug_api.lua/demo_minimal.lua 完全无引用），同步 3 测试体（test_kag_core 去 Parser/Conductor 节、test_integration 去文件清单项、g9_test 改 require scheduler）；**冲突解决**：web/scripts-index.json 预加载列表致 web 套件 ENOENT——重生成索引（73 模块）后 76/76 绿 ②**存储加密往返 +8**——SaveManager 层加密（CAES 魔数+nonce+tag+GCM，ISaveProvider 无加密意识）：密文不可读/无钥优雅失败/错钥 GCM 拒/明文经魔数门控直通（安全自动检测）/伪造魔数拒/篡改密文+nonce 拒/槽边界镜像——引擎零缺陷 ③**纹理预算强制 +5**——di 层解耦（ITextureBudget tier 0-5 + SandboxQuota）：tier 边界精确映射/tier5 仅 override/quota 满拒+release 恢复/quota 0 全拒；记录 P2 detect RAM 边界不可注入、P3 quota 缺省不对称 ④**ks_check 第 4 轮 +4 警告**——if/while/until 缺空 exp=、单标签重复参数（后者胜）、未被引用 label（强保守：registry 门禁+非入口+前置跳转；豁免入口/流穿/sel 引用/end 后锚点）、[macro goto] 死宏；demo 18 场景零 WARN（55 断言）⑤**web 全教程回归 sweep**——参数化 15 场景（tutorial 01-13 + showcase + example_game）全部 DONE 零错误：tutorial_05 分支收敛断言、tutorial_13 palette 恰好 3 事件、story.ks 好结局路线；web 实现零缺口（62→76）⑥**G4-2 SceneOutline 接线**——SceneOutlinePanel 读 store 活动文档渲染大纲，label 点击走既有 revealEditorLine/revealRequest/inspected 导航（SceneTree 同款），editor 205→210 | web 76/76, editor 210/210, Lua 121/121, 孤儿 16/16, C++ 769/769, ctest 10+AI 跳过, 耦合/覆盖 PASS, RPC smoke 45/45 | (round 79 提交) |
| 78 | G10/G11 硬化 + G4 第一步 + 脚本边缘锁定（6 子代理）：①**audio 预初始化一致性修复**——setGlobalVolume 未 init 存值而 setBusVolume 静默丢弃（init 还硬编码 1.0f）：改无条件存成员 + init() 用存储值落 bus，+4 测试（init 前设 0.42 init 后生效/回归/即时/静音）②**platform/input 边界 +8**——focus 路由互斥（KAG 只收 KEY_DOWN+MOUSE_BUTTON_DOWN，motion 有意过滤）、focus 往返无 phantom click、窗口 resize 映射、修饰键透传、getTicksMs 单调（帧 delta clamp 在 entry 模块内联，平台接口不可测——留主 agent）③**CARC 归档边界 +8**——多文件往返/body+header 篡改均被签名拒/AES-GCM 密钥分离/Ed25519 有效与篡改/2MiB 流式/空归档缺陷锁定（IArchiveWriter 拒绝零文件 finalize）/路径无规范化按字节键（行为记录）④**脚本流控边缘 +28**——嵌套 switch 深度 3、break/continue 最内层循环（含 switch 内 break 破外层循环）、until 大 timeout 立即真零等待、[for] 浮点步进精确次数、**step=0 钳位为 1（非报错，锁定实测）**、负步进倒序、while 体内条件翻假体完即出、**[goto] 跳入循环体=未定义（绕过循环头、endfor 无栈变 no-op、计数器 nil——锁定不崩）**⑤**web 语言热切换全链路**——[i18n] 后已显示页 draws 重放重本地化/choice 标签重本地化/backlog 条目重本地化（+3，零桥改动——relocalize_page 真实工作）⑥**editor SceneOutline 第一步（G4）**——纯解析器 parseSceneOutline（label/command/text）+ buildOutlineSections + 只读大纲组件（label 点击导航 onSelectLabel），20 文件 205/205，tsc 干净；下一步：per-label 时间线 + /api/pick 集成接线 | web 62/62, editor 205/205, Lua 121/121, 孤儿 16/16, C++ 755/755, ctest 10+AI 跳过, 耦合/覆盖 PASS, RPC smoke 45/45 | (round 78 提交) |
| 77 | G10 硬化 + web 能力补齐 + CI 修复闭环（6 子代理 + 主代理 CI 修复）：①**web palette LUT 绑定**——wasmoon 把 JS 对象封装成 userdata 非 table，palette.lua 的 lut_available 守卫永远降级、backend.load_image 崩溃：bridge 重包装 _G.backend 为真 Lua 表 + load_image/set_palette 桥接 + DOM CSS 调色滤镜（day 中性/night 蓝暗按强度），tutorial_13 palette 全链路无降级（54→59）②**web [i18n]/[goto] parity**——i18n 曾被 JS stub 遮蔽真实模块（wasmoon 封 userdata 使 set_language 检查失败）：改加载真实 i18n.lua；goto 纯 Lua flow 分支零改动；+3 用例（含 language= 缺失的 schema 级错误与 VM 存活）③**C++ audio 边界 +8**（BGM/SE 独立、4-slot voice 池轮转实测、bus 音量权威值、无效句柄 no-op、suspend/resume）——发现 setGlobalVolume 未 init 存值 vs setBusVolume 静默丢弃的不一致（记录未修）④**C++ resource 边界 +8**（provider 链回退/优先级/空读穿透、AsyncLoader 完成/失败/cancelAll/去重缓存）——引擎零缺陷⑤**ks_check 跨场景**——[call]/[jump] 目标 .ks 存在性注册表（ls/dir 扫描，扫描失败静默跳过零误报）、空 target 警告、宏定义遮蔽内建 flow 命令警告（死宏）；demo 18 场景零 WARN（28→41 断言）⑥**editor +11**（[i18n] 补全=language= 唯一、lang= unknown-param 锁定、i18n 高亮补齐、goto 补全）；**CI 修复**——r76 四平台全红：macOS/Linux 因 test_i18n 的 cli --missing 子进程找不到解释器（external/lua 是源码树、POSIX popen 空串输出空报告），Windows 因 check_test_coverage 报编辑器高亮缺 i18n 契约；修复：解释器解析用 SAVE_ARG[-1]（CLI e2e 会覆盖 _G.arg 故需预先保存）+ lua5.4 候选 + Windows call "..." 包裹（本地路径含括号 Caesura(AmeKAG) 分词截断——孤儿 runner 同款坑）+ 编辑器 i18n 高亮；修复后 CI 全绿 | web 59/59, editor 188/188, Lua 121/121, 孤儿 16/16, C++ 735/735, ctest 10+AI 跳过, 耦合/覆盖 PASS, RPC smoke 45/45, CI 5/5 绿 | (round 77 提交) |
| 76 | 新命令接线 + 六路扇出（6 子代理）：**主代理** ①**[i18n language=] 命令**——运行时热切换语言（i18n.set_language 回退链 + relocalize_page 全页重绘，settings 同款路径），kag.lua 自动注册，契约+test_i18n_cmd 11 断言，契约文档再生成 ②**[goto] 原生别名**——KAG3 goto 之前仅导入时改写、运行时未知命令渲染为文本：调度器 jump 分支接受 goto（flow_commands 同步）、lsp FLOW_CMDS、ks_check 双清单，test_scheduler +3（含裸 *label 跳转/缺目标不崩）③**rollback 与提升循环栈验证兼容**（快照仅含结构性字段，迭代计数在 f 中随回滚恢复，无需修复）；**子代理** ④**C++ storage 硬化测试 +8 用例**（corrupt save 优雅恢复/slot 0/99 边界/覆盖语义/迁移链 v1→v5/嵌套载荷往返）——SaveManager 实现零缺陷（711→719）⑤**ks_check structuralWarnings 接线**——发现其定义后从未被调用（死代码），接入 checkScene + 导出，新增 5 类警告：重复 label、[for] 体永不执行、宏遮蔽内置、无条件 jump 后不可达、孤儿闭合符（demo 零误报）⑥**运行时契约测试 +18**（math 链/textspeed-cps 字段/preload 枚举拒绝/sel x= 全管线/嵌套宏 e2e/深度守卫 1002）⑦**web parity 2 轮 +5 用例**（while/textspeed-cps/button cond 过滤/save-in-for load 续跑/endselect）——无需桥改动（web 加载真实 save.lua，循环恢复天然继承）⑧**editor +10**（sel 别名补全桥探针/KAG3 参数诊断转发锁定/button x hover/goto+math 高亮）⑨**docs 同步**（cpp-interfaces 渲染 7 接口/lua-modules i18n API/能力矩阵/kag-commands goto/nextgen 标准/sample-library） | web 54/54, editor 177/177, Lua 121/121, 孤儿 16/16, C++ 719/719, ctest 10+AI 跳过, 耦合/覆盖 PASS, RPC smoke 45/45 | (round 76 提交) |
| 75 | 三缺陷修复 + 六路扇出（6 子代理）：**主代理 scheduler/compiler 修复** ①**save/load 循环恢复**——for/while/if/switch 四栈从 run 局部提升至 ctx（_forStack/_whileStack/_ifStack/_switchStack），capture_state 序列化 loop_stacks，load 经 ctx._resumeLoopStacks marker 在重开 run 入口恢复：**[for]/[while] 体内 [save]→[load] 后循环续跑至完成**（round-74 缺陷关闭；test_saveflow 锁定 for 恢复 iter_done==4 + while 恢复 hp==5，无 marker 的旧行为保留兼容断言）②**宏展开守卫改为深度制**——旧按 ctx 累计计数器使 1000+ 合法宏调用（如 [for] 内每迭代调宏）误报递归；改为 splice 深度栈（push endIdx、body 完成后在循环底部 pop、递归同一位置重复 splice 不弹出）深度>100 报错，1002 顺序调用与 2000 迭代循环均通过 ③**嵌套宏定义支持**——scheduler/compiler 的 macro 体收集改深度计数（[macro outer][macro inner]...[endmacro][endmacro] 不再截断流；compiler 对含嵌套定义的宏保守标记 redef 走运行期）；**子代理** ④web parity 3 缺陷修复（layers.ensure 签名/choice 点击分发/_pendingJump 路由）+5 flow 测试 44→49 ⑤editor LSP 桥与高亮 133→167（含 round-71-74 新命令探针）⑥嵌套花括号插值回归（确认 match_brace 早已平衡匹配，+7 锁定）⑦i18n 运行时 API（set_language/current_language/translate 占位符/reload，+21）⑧kag3_import 宏参数 & 转换（&N→%N%、[goto]→[jump] 别名、iscript/emb 回归，+24）⑨headless RPC smoke +14（math 命令 eval 读回/save 槽往返/契约计数）⑩LSP [sel] 别名参数补全（sel 共享 button handler 但无契约——lsp 别名映射，test_lsp 77） | web 49/49, editor 167/167, Lua 120/120, 孤儿 16/16, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS, RPC smoke 45/45 | (round 75 提交) |
| 74 | 阶段 D 收官六路扇出（6 子代理）：①**[sel]/[button] x= 结果捕获**（KAG3 [sel x="tf.result"] 兼容：button 契约新增 x 参数，[endbutton] 选中后将目标标签写入声明的 tf./sf./f./mp./lf. 作用域，裸键回退 f 作用域与 [set] 对齐；test_choice/test_select +189 行）②**ruby 游标跟随修复**（schema.coerce 填 x=0/y=0 默认而 Lua 0 为真值——裸 [ruby] 被钉在绝对 (0,0) 而非跟随文本游标；仅严格正 x/y 覆盖游标跟随）③**nvl 层显隐恢复**（[nvl] 隐藏 _textbox/_nameplate 并保存先前可见性，[nvl off] 恢复；test_nvl/test_text_markup +212 行）④**LSP 定义/引用导航深度**（命名引号 target="*label" 光标在值内解析 + 多导航点聚合 1 定义+4 引用，test_lsp 70 断言，实现已验证完备）⑤**存档/读档边界**（saveplace 在 call 帧内记录内帧 scene+token_idx 不序列化调用栈/loadplace 经 _pendingJump 精确恢复无 off-by-one/save 在 for 内计数器入 f 表；**缺陷锁定（仅报告未修，scheduler 受保护）：for 内 save→load 恢复后循环栈为空提前退出**/listsaves 空槽同步进 tf/slot 0..99 钳制/槽外部删除 load_result=error 不抛）⑥**宏系统深度**（自/互递归 1000 预算报错不挂死/参数透传占位缺失保留字面量/嵌套调用正常/宏体内 [jump]/[return]/宏覆盖内置非对称；**两缺陷仅报告：展开预算按 ctx 累计永不重置 1002 次合法调用误报、嵌套宏定义被朴素 endmacro 扫描破坏**）⑦**i18n CLI 端到端**（ks_i18n 导入导出链路 75 断言 + docs/guides/i18n.md 新指南） | web 44/44, Lua 120/120, 孤儿 16/16, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 74 提交) |
| 73 | 脚本深度：控制流边界 + 静态分析 + 导入端到端 + 性能基线（6 子代理）：①**switch 语义边界**（test_switch +206 行：tostring 等值跨类型匹配/插值 case 字面量/重复键后者胜/default 位置/嵌套 if/break 无循环抛错）——修正手建流原生值驱动为 tokenizer 管线驱动（主代理裁决：实现正确，真实管线 1/1.0/true/字符串全匹配）②**循环范围边界**（test_for/while/loop_control +27 断言：插值边界不展开是文档化行为/变量边界/倒序/10000 次与 65536 守卫不误伤/嵌套 continue）③**ks_check 结构警告**（悬空 jump/call/link 目标、未用 macro、case 重复——[WARN] 不改变退出码）④**LSP unknown-param 诊断**（错拼参数 severity=2，排除位置槽/点号路径/_ 元数据/flow 命令）⑤**kag3_import 未加引号参数别名修复**（var=f.x 裸值形式之前不重写键→转换后运行时静默失效）+ 端到端集成测试（转换→编译→真实运行 22 断言，孤儿套件）⑥**性能基线**（round-66 预算余量 >98%，新混合表达式/add 链 <2s 预算断言加入 frame_bench）⑦tutorial_13 启用 palette 演示（round-72 守卫后安全降级） | web 44/44, Lua 120/120, 孤儿 15/15, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 73 提交) |
| 72 | KAG3 兼容命令的教学与导入闭环（6 子代理扇出）：①**kag3_import 接线**——预注册 math/character/resource 模块（headless lint 不再误报 UNSUPPORTED）；PARAM_ALIASES 机制（add/sub/mul/div/mod/dec 的 var→name、csp/csl 的 left/top→x/y）；CONFLICT_NOTES（[palette] KAG3 索引调色盘 vs 引擎 LUT 语义差异输出非阻断 NOTE）②**教程 13**——tutorial_13_commands.ks 实战新命令（textspeed/cps 切换、算术链 + 插值显示、csp/csd/csl、notify、vibrate、preload + bg 切换），注册进 web flow 测试（01-13）与 sample-library ③**修复**——[palette] 运行时必崩（backend.set_palette/destroy_texture 绑定全库不存在）：palette.lua 加 lut_available 守卫降级可见提示；[preload] 显式 type/wait 参数挂 ks_check（choices 数组形式与 schema.coerce 字典查找不匹配）：改 enum values 字典形式 ④**文档同步**——sample-library 过时清单（showcase 实际命令、覆盖矩阵去 playvoice、教程 01→13）、教程 06/08 收尾叙述改中性、表达式文档 [eval] 裸值表达式契约（实测修正：裸调用语句不入 eval_result，仅值表达式内调用可）、cpp-interfaces/kag-commands 两处 stale ⑤**深度测试**——test_textspeed.lua（27 断言：handler 写穿 + kag_runner 真实逐字 reveal/skip/auto 全链路，孤儿套件）、kag3_import 69 断言、headless smoke 加插值诊断断言、web flow 端到端 10/10（含教程 13 播放）⑥**收尾复检**——契约 117/117 零漂移、LSP/schema_doc 11 模块、高亮 117/117、调度器可达 10/10 | web 44/44, Lua 120/120, 孤儿 14/14, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 72 提交) |
| 71 | KAG3 兼容命令补齐 + LSP 插值诊断（G9 主线，7 子代理扇出）：
①**新命令**——算术 [add]/[sub]/[mul]/[div]/[mod]/[dec]（commands/math.lua，镜像 [inc] 语义/除零可见报错）、
角色 [csp]/[csd]/[csl]（commands/character.lua，镜像 [image] 路径解析）、
文本速度 [textspeed]/[cps]（接入 kag_runner 的 ctx.text_speed 真实读取点，50cps=KAG3 默认）、
效果 [palette]/[vibrate]（路由 palette 模块与 [vib] 别名）、[notify]（toast 模块，headless 降级）
②**接线**——kag.lua 注册 math/character；init.lua 预加载 palette（沙箱 require 规则）；resource.lua 补 [preload] 契约；
schema_doc/lsp 模块清单 +3；kag3_import 修 notif/monocro 坏建议；编辑器高亮 +14 命令
③**LSP 深化**——schema.lua 导出纯增量 Schema.checkInterp（复用运行期编译路径），
lsp diagnostics 扫描 \${} 插值（severity 1/2），completion 增加变量前缀提示（cursorCol 可选参数，协议兼容），hover 表达式速查卡
④**修复**——scheduler [eval] 裸值表达式 return 包装（文档契约 tf.eval_result 成立）；
test_carc_import os.exit(0) 静默杀套件 bug；test_i18n 改仓库内 tmp 目录（scan_dir C6 白名单拒绝反斜杠路径）；
runner 预加载 palette（修 vfx_clamp 沙箱 require 中止导致 package.loaded mock 污染级联）
 | web 133/133, Lua 120/120, 孤儿 13/13, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 71 提交) |
| 70 | 脚本鲁棒性与 CI 保鲜修复（审查 C1/C2）：①**长字符串感知扫描**——find_top/match_colon 遇 [[...]] 后落到收尾 ']' 使 depth 变负，后续 '?' 永不匹配（f.s == [[x]t]] ? 1 : 2 不翻译）；修正为 i=ls 精确越过字面量；translate_operators/translateAssignment 同步跳过长字符串（内部 && || != ?? 与 '=' 是内容非运算符）；修正旧 C2 测试对未闭合索引的错误断言 + 深度 0 长字符串回归（120/120）②**lua_cli 解释器构建**（external/lua 静态库 + lua.c，build/lua/<cfg>/lua.exe；MSVC LNK1149 改名 lua_lib.lib；api_stats 探测 build 下解释器）③**CI 保鲜守卫确定性化**——api-stats.md 移除环境相关测试计数行（FFmpeg/GPU/负载依赖，Windows CI 重生成与本地不一致）：测试健康由 ctest/Lua suites/check_test_coverage 专属门禁保证 ④**Linux -lm 链接修复**（lua_cli lvm.c pow/floor 未定义引用）⑤注册 saveflow + bench_dispatch 进孤儿套件 | web 44/44, Lua 120/120, 孤儿 11/11, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 70 提交) |

| 69 | 表达式语言文档全面更新：kag-expression-language.md 补齐 round 55-68 能力——三元可在 [ ] 索引与 ( ) 组内（find_top 深度限制的历史缺口已修）、插值 ${expr} 平衡花括号与长括号、[switch exp=] 选择器（tostring 等值/数值/布尔/default）、[eval] 语句节（运算符翻译 + 三元 RHS 赋值 + 表构造器，round 61/68）；修正过时示例（[set value=] 不求值 → [ch text=] 插值）；全部示例均有测试背书 | web 44/44, Lua 120/120, 孤儿 9/9, C++ 711/711, ctest 10+AI 跳过, 耦合/覆盖 PASS | (round 69 提交) |
