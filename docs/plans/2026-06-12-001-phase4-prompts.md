# Phase 4 执行提示词

## 通用前置约束

```
你是 Caesura (AmeKAG) galgame 引擎的开发者。先读 AGENTS.md 理解核心架构约束。

关键规则：
- 所有后端访问走 BackendRegistry::instance().get*()
- 新接口放 src/<module>/api/I*.h，纯虚类，=0 方法
- 禁止跨模块引用具体头文件（组合根 entry/ 除外）
- 一步一提交，每步完成后构建零错误 + 测试全绿
- 构建：cd build && cmake --build . --config Debug --parallel
- 测试：cd build\tests\Debug && .\CaesuraTests.exe
```

---

## M10 — rpc 模块完善

```
## 任务：rpc 模块完善（M10）

### 当前状态
- 2 cpp + 2 h：EditorServer, RpcServer
- 无 api/ 目录，无测试文件
- EditorServer 单例，内嵌 HTTP 服务器（cpp-httplib）
- RpcServer 单例，提供 JSON-RPC 端点
- 已在上一轮解除 entry 依赖（EditorServer.cpp 不再 include Engine.h）
- BackendRegistry 中未注册 rpc 接口

### 执行步骤

#### Step 1: 提取接口
- 创建 src/rpc/api/IRpcServer.h
  - RpcServer 公共方法：start, stop, isRunning, port
  - 不暴露 Lua 相关方法（那是内部实现）
- 创建 src/rpc/api/IEditorServer.h
  - EditorServer 公共方法：start, stop, isRunning, port, setLuaState, setWebRoot, pushLog
- RpcServer 继承 IRpcServer
- EditorServer 继承 IEditorServer
- 提交

#### Step 2: 注册到 BackendRegistry
- BackendRegistry 添加 setRpcServer/getRpcServer
- BackendRegistry 添加 setEditorServer/getEditorServer
- Engine::init() 中注册（如果启动了编辑器模式）
- 确认 rpc/ 下所有外部访问走 BackendRegistry
- 提交

#### Step 3: 添加测试
- 创建 tests/cpp/test_rpc.cpp
- 测试用例（至少 4 个）：
  1. RpcServer 构造/start/stop 不崩溃
  2. RpcServer 重复 start 返回 false
  3. RpcServer 重复 stop 不崩溃
  4. EditorServer 构造/setWebRoot/pushLog 不崩溃
  5. EditorServer start 在端口被占用时返回 false
- 注意：不测试实际 HTTP 请求（需要网络和端口），只测生命周期
- 构建 + 测试全绿后提交

### 预期产出
- src/rpc/api/IRpcServer.h
- src/rpc/api/IEditorServer.h
- tests/cpp/test_rpc.cpp
- 3 次提交
```

---

## M11 — job 模块完善

```
## 任务：job 模块完善（M11）

### 当前状态
- 1 cpp + 1 h：JobSystem（单例）
- 无 api/ 目录
- 有 tests/cpp/test_job_system.cpp
- JobSystem 提供：init, shutdown, submit, waitAll, pendingCount
- render 和 resource 模块直接 include job/JobSystem.h
- BackendRegistry 中未注册

### 执行步骤

#### Step 1: 提取 IJobSystem 接口
- 创建 src/job/api/IJobSystem.h
- JobSystem 公共方法映射：
  virtual void init() = 0;
  virtual void shutdown() = 0;
  virtual void submit(std::function<void()> task) = 0;
  virtual void waitAll() = 0;
  virtual int  pendingCount() const = 0;
  virtual bool isRunning() const = 0;
- JobSystem 继承 IJobSystem，添加 override
- 提交

#### Step 2: 注册到 BackendRegistry
- BackendRegistry 添加 setJobSystem/getJobSystem
- Engine::init() 中注册（在 JobSystem::instance().init() 之后）
- 更新 render/ParticleSystem.cpp, render/VideoPlayer.cpp, resource/AsyncLoader.cpp
  — 这些文件直接 include job/JobSystem.h
  — 改为通过 BackendRegistry::instance().getJobSystem() 访问
  — 移除直接的 JobSystem.h include
- 提交

#### Step 3: 扩展测试
- 读取 tests/cpp/test_job_system.cpp，评估当前覆盖
- 添加测试用例：
  1. 提交空任务不崩溃
  2. 提交 10 个任务，waitAll 后全部完成
  3. shutdown 前未完成的任务被丢弃（不崩溃）
  4. IJobSystem 接口完整性（虚函数表检查）
- 构建 + 测试全绿后提交

### 预期产出
- src/job/api/IJobSystem.h
- BackendRegistry 注册 JobSystem
- render/resource 模块不再直接 include JobSystem.h
- 3 次提交
```

---

## M12 — di 模块完善

```
## 任务：di 模块完善（M12）

### 当前状态
- 3 cpp + 5 h：BackendRegistry, SandboxQuota, TextureBudget, ThreadAssert
- BackendRegistry 已全接口化 ✅
- ITextureBudget 在 api/ ✅
- 无测试文件
- SandboxQuota 是 namespace + 静态方法，已通过 BackendRegistry 封装

### 执行步骤

#### Step 1: SandboxQuota 接口化
- 创建 src/di/api/ISandboxQuota.h
- 当前 SandboxQuota 是 namespace 下的静态方法：
  bool tryAlloc(lua_State* L, const char* kind);
  void release(lua_State* L, const char* kind);
  int count(lua_State* L, const char* kind);
  int maxLimit(lua_State* L, const char* kind);
- 改为实例接口（内部持有 lua_State*）：
  virtual bool tryAlloc(const char* kind) = 0;
  virtual void release(const char* kind) = 0;
  virtual int  count(const char* kind) = 0;
  virtual int  maxLimit(const char* kind) = 0;
- 创建 SandboxQuotaImpl 类实现 ISandboxQuota
- 注册到 BackendRegistry（替代当前的 tryAlloc/release 内联方法）
- BackendRegistry 的 tryAlloc/release 改为委托 m_sandboxQuota
- 提交

#### Step 2: ThreadAssert 规范化
- 读取 src/di/thread/ThreadAssert.h
- 当前是头文件宏/内联函数，用于线程断言
- 确认不需要接口（这是编译时工具，不是运行时后端）
- 添加文档注释说明用途和使用场景
- 提交

#### Step 3: 添加 BackendRegistry 测试
- 创建 tests/cpp/test_backend_registry.cpp
- 测试用例（至少 5 个）：
  1. BackendRegistry 单例访问
  2. setRenderDevice/getRenderDevice 往返
  3. setAudioBackend 后 createAudioBackend 按名称查找
  4. registerNullBackends 后 getRenderDevice 非空
  5. setLuaState 后 tryAlloc/release 不崩溃
  6. ResourceHandle isValidHandle 无效 handle 返回 false
  7. invalidateHandles 后 handle 失效
- 构建 + 测试全绿后提交

#### Step 4: 添加 TextureBudget 测试
- 创建 tests/cpp/test_texture_budget.cpp（如果已存在则扩展）
- 测试用例：
  1. TextureBudget 默认 tier 为 1
  2. setTier(3) 后 getTier 返回 3
  3. getBudgetMB 在不同 tier 返回合理值（128/256/512/1024/2048/4096）
  4. isAutoDetected 初始为 true，setTier 后为 false
  5. getTierName 返回非空字符串
- 构建 + 测试全绿后提交

### 预期产出
- src/di/api/ISandboxQuota.h
- tests/cpp/test_backend_registry.cpp
- tests/cpp/test_texture_budget.cpp（扩展）
- 4 次提交
```

---

## M13 — audio 模块完善

```
## 任务：audio 模块完善（M13）

### 当前状态
- 2 cpp + 3 h：SoLoudAudioEngine, NullAudioBackend, IAudioBackend(api/)
- IAudioBackend 接口已就位 ✅
- 有 tests/cpp/test_audio.cpp（11 个 TEST_CASE）
- SoLoudAudioEngine 使用 SandboxQuota 进行资源配额（已通过 BackendRegistry）
- 已在上一轮解除 LuaManager 直接依赖

### 执行步骤

#### Step 1: 审查 SoLoudAudioEngine 资源管理
- 检查 SoLoudAudioEngine.cpp 中的资源配额调用
  - 确认所有 tryAlloc/release 成对出现
  - 确认异常路径上正确释放配额
- 检查音频句柄泄漏：
  - playBGM/playSE/playVoice 返回的句柄是否在 stop*/shutdown 时正确释放
  - LRU 缓存在多线程下的安全性（SoLoud 内部线程安全，确认我们这边没问题）
- 提交

#### Step 2: 补充边界测试
- 扩展 tests/cpp/test_audio.cpp
- 添加测试用例（至少 4 个）：
  1. playSE 无效文件返回 0（已有）— 补充：之后调 stopSE 不崩溃
  2. setGlobalVolume 0.0 → getGlobalVolume 返回 0.0
  3. setGlobalVolume 2.0 → 被 clamp 到 1.0
  4. 未 init 时调用 playSE 返回 0，不崩溃
  5. shutdown 后再次 init 成功（重新初始化）
  6. fadeVolume 负值不崩溃
  7. 大量 playSE 调用（100次）不泄漏（检查 activeVoiceCount）
- 构建 + 测试全绿后提交

#### Step 3: IAudioBackend 接口完整性检查
- 确认所有 24 个纯虚方法在 SoLoudAudioEngine 和 NullAudioBackend 中都有实现
- 检查 BackendRegistry 中的 getAudioBackend() 返回类型为 IAudioBackend*
- 确认无外部代码绕过 BackendRegistry 直接使用 SoLoudAudioEngine 单例
  （Engine.cpp 中的 static_cast<SoLoudAudioEngine*> 除外——组合根允许）
- 提交

### 预期产出
- tests/cpp/test_audio.cpp（扩展，从 11 → 18+ 测试用例）
- 3 次提交
```

---

## 执行顺序

Phase 4 四个模块相互独立，可任意顺序执行：

```
M10 (rpc) ─┐
M11 (job) ─┼── 并行可做
M12 (di)  ─┤
M13 (audio)─┘
```

推荐顺序：M11(job) → M12(di) → M10(rpc) → M13(audio)
（job 接口化后 render/resource 可立即改用 BackendRegistry）

## 最终验收

Phase 1-4 全部完成后：

```
预期测试总数：    149 → 180+
预期 api/ 文件：   11 → 20+
0 个桩实现残留：   3(NullBackend 仅作为 fallback 保留，非唯一实现)
耦合度：          所有非 entry 模块头文件 0 跨模块具体依赖
```
