# S1-S5 执行提示词

## 通用前置约束

```
你是 Caesura (AmeKAG) galgame 引擎的开发者。先读 AGENTS.md。

关键规则：
- 所有后端访问走 BackendRegistry::instance().get*()
- 一步一提交，构建零错误 + 257 测试全绿
- 构建：cd build && cmake --build . --config Debug --parallel
- 测试：cd build\tests\Debug && .\CaesuraTests.exe
```

---

## S1 — Demo 成为真正的 Galgame

```
### S1.1: Demo KAG 脚本完善

当前状态：scripts/demo_story.ks 仅有语法解析测试。未端到端运行。

Step 1: 扩展 demo_story.ks
- 读取 scripts/demo_story.ks 和 scripts/demo.lua
- 扩展为至少 50 行的完整脚本：
  *start
  显示标题（@text "Caesura Demo"）
  进入场景1（@bg "bg_classroom"）
  对话（@text 3-4 行）
  BGM 播放（@bgm "bgm_01"）
  选项分支（@choice "去走廊" "留在教室"）
  分支A（@jump label_corridor）
  分支B（@jump label_classroom）
  结局画面
  @end
- 所有资源引用使用不存在的占位符（Demo 不依赖真实文件）
- 提交

Step 2: Demo 执行时资源降级
- 当 KAG 命令引用的资源不存在时（bg/playbgm 等），不崩溃，打印日志
- 检查 KAGBinding.cpp / conductor.lua 中的资源加载路径
- 提交

预期产出：Demo 脚本代表完整 galgame 流程。2 次提交。

---

### S1.2: Demo 端到端运行测试

Step 1: 创建 tests/cpp/test_demo_e2e.cpp
- 初始化 LuaManager → 加载 demo_story.ks → 执行
- 测试用例：
  1. Demo 加载不崩溃
  2. Demo 执行 30 帧不崩溃
  3. Demo 至少触发了 text/bg/choice 命令
  4. @choice 选项回调被注册
  5. Demo 执行到 @end 正常终止
- 更新 tests/CMakeLists.txt
- 提交

预期产出：1 次提交。

---

### S1.3: Demo 资源生成脚本

Step 1: 创建 scripts/generate_demo_assets.lua
- 生成 640×480 纯色 PNG（背景用）
- 生成 1 秒静音 WAV（BGM/SE 用）
- 放 tests/scripts/demo_assets/ 目录
- 提交

预期产出：1 次提交。
```

---

## S2 — 剩余模块测试补齐

```
### S2.1: tests/cpp/test_di.cpp

Step 1: BackendRegistry 测试
- 创建文件，测试用例：
  1. BackendRegistry 单例访问（&a == &b）
  2. setRenderDevice/getRenderDevice 往返
  3. setAudioBackend/getAudioBackend 往返
  4. registerNullBackends 后所有 getter 非空
  5. setLuaState/getLuaState 往返
  6. tryAlloc/release 不崩溃
  7. ResourceHandle isValidHandle/makeHandle/invalidateHandles
  8. 未注册的 getter 返回 nullptr
- 更新 CMakeLists.txt
- 提交

### S2.2: tests/cpp/test_minigame.cpp

Step 1: 检查现有 tests/cpp/test_mini_game.cpp
- 确认测试是否在运行（不是 skipped）
- 如果内容不足，扩展：
  1. NullMiniGameBackend 所有方法返回安全值
  2. BgfxMiniGameBackend 构造不崩溃
  3. IMiniGameBackend 接口完整性
- 提交

### S2.3: tests/cpp/test_platform.cpp

Step 1: SDL3PlatformBackend 测试
- 创建文件，测试用例：
  1. SDL3PlatformBackend 构造不崩溃
  2. MobileAdapter 触摸→鼠标映射
  3. MobileAdapter 暂停/恢复状态
  4. IPlatformBackend 接口完整性
- 更新 CMakeLists.txt
- 提交

### S2.4: tests/cpp/test_storage.cpp

Step 1: SaveManager 测试
- 创建文件，测试用例：
  1. SaveManager 单例访问
  2. init 临时目录成功
  3. save→load 数据往返
  4. listSaves 返回正确列表
  5. deleteSlot 后不存在
- 更新 CMakeLists.txt
- 提交
```

---

## S3 — Engine 可测试性修复

```
### S3.1: 修复 Engine 默认构造崩溃

当前问题：Engine 构造函数创建 GpuMonitor → 无 GPU 环境 SIGSEGV。

Step 1: 诊断崩溃点
- 在 tests/cpp/test_entry.cpp 中尝试构造 Engine
- 观察 SIGSEGV 调用栈
- 确认崩溃点（大概率在 GpuMonitor 构造）

Step 2: 延迟 GpuMonitor 创建
- Engine 构造函数中不创建 GpuMonitor
- 移到 initPlatformPhase() 中创建
- 或：添加 headless 模式检查（headless=true 时跳过 GPU 相关创建）
- 提交

### S3.2: 添加 Engine 构造测试

Step 1: 扩展 tests/cpp/test_entry.cpp
- 测试用例：
  1. Engine 默认构造不崩溃
  2. Engine 构造后 shutdown 不崩溃（不 init）
  3. Engine headless init → shutdown 生命周期
- 提交
```

---

## S4 — CI 三端验证

```
### S4.1: 触发 CI

Step 1: 推送当前 master 到 GitHub
- git push origin master
- 等待 GitHub Actions 完成

Step 2-3: 根据失败日志迭代修复
- macOS：brew 依赖、Metal 后端
- Linux：apt 依赖、OpenGL 后端
- 每修复一个错误 → 提交 → 推送 → 观察

Step 4: 三端全绿后
- README.md 更新 CI badge
- 提交
```

---

## S5 — 编辑器收尾

```
### S5.1: Live2D 面板

Step 1: 添加 models 列表端点
- EditorServer 添加 GET /api/live2d/models
- 扫描 models/ 目录返回 .moc 文件列表
- 添加测试

Step 2: 添加 load 端点
- EditorServer 添加 POST /api/live2d/load {"modelPath":"..."}
- 调用 AnimationBackend::loadModel
- 添加测试

Step 3: 前端面板
- 修改 Live2DPanel.jsx：显示模型列表 + 点击加载
- 提交

### S5.2: 一键打包

Step 1: 添加 build 端点
- EditorServer 添加 POST /api/build
- 调用 CARCWriter 打包 scripts/ + assets/
- 返回文件路径和大小

Step 2: 前端按钮
- 修改 AssetPanel.jsx：添加 Build 按钮
- 调用 /api/build → 显示结果
- 提交
```