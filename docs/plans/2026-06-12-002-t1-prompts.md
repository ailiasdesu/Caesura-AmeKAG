# T1 — 端到端集成测试提示词

## 通用前置约束

```
你是 Caesura (AmeKAG) galgame 引擎的开发者。先读 AGENTS.md。

关键规则：
- 所有后端访问走 BackendRegistry::instance().get*()
- 新测试放 tests/cpp/，更新 tests/CMakeLists.txt
- 一步一提交，构建零错误 + 测试全绿
- 构建：cd build && cmake --build . --config Debug --parallel
- 测试：cd build\tests\Debug && .\CaesuraTests.exe
- 当前基线：193/193 测试全绿
```

---

## T1.1 — KAG 脚本解析→执行集成测试

```
### 任务：KAG 脚本解析→执行集成测试

### 当前状态
- test_kag_binding.cpp + test_kag_integration.cpp 已存在
- KAG 命令分散在 scripts/kag/commands/（audio/layer/resource/save/system/text/transition/vfx/video 共 9 个文件）
- 测试中 Lua VM 初始化可用（LuaManager::init）

### 执行

#### Step 1: 创建 KAG 集成测试文件
- 创建 tests/cpp/test_kag_execution.cpp
- 初始化 LuaManager → 注册 KAG 模块 → 执行脚本字符串
- 测试用例（至少 5 个）：
  1. 加载空脚本不崩溃
  2. 执行 `@text "hello"` 命令（KAG 文本显示）
  3. 执行 `@bg "scene.png"` 命令（背景设置）
  4. 执行 `@wait 0.1`（等待命令）
  5. 连续执行多个命令不泄漏（10 个 @text → 检查 Lua 栈干净）
- 更新 tests/CMakeLists.txt
- 构建 + 测试全绿后提交

#### Step 2: KAG 分支/选项测试
- 添加测试用例：
  1. 条件分支 @if 正确跳转
  2. @jump 跳转到 label
  3. @choice 选项注册
- 提交

### 预期产出
- tests/cpp/test_kag_execution.cpp（8+ 用例）
- 2 次提交
```

---

## T1.2 — 渲染管线集成测试

```
### 任务：渲染管线集成测试

### 当前状态
- test_render_device.cpp + test_render_pipeline.cpp 存在
- BgfxRenderDevice 在无窗口环境下可构造（默认 1280×720）
- LayerManager/TextureManager/ParticleSystem 均可实例化
- 集成测试需要 bgfx 初始化，headless 模式下可用

### 执行

#### Step 1: 创建渲染集成测试
- 创建 tests/cpp/test_render_integration.cpp
- 测试用例（至少 4 个）：
  1. init → beginFrame → endFrame → shutdown 完整帧循环不崩溃（headless 模式）
  2. Layer 设置纹理 → setVisible → markDirty → render 不崩溃
  3. ParticleSystem 创建发射器 → emit → update → render 粒子可见
  4. RTT 创建 → fillViewport → blit 到默认 framebuffer
- 注意：所有渲染测试在 headless 模式下（bgfx 无原生窗口）
- 更新 tests/CMakeLists.txt
- 构建 + 测试全绿后提交

#### Step 2: 纹理/Shader 集成测试
- 添加测试用例：
  1. TextureManager 创建纯色纹理 → 渲染到 RTT → 验证 RTT 非空
  2. ShaderCache registerProgram → 查找 → 返回正确 handle
  3. FreeTypeContext 初始化 → 渲染文字 → 验证不崩溃
- 提交

### 预期产出
- tests/cpp/test_render_integration.cpp（7+ 用例）
- 2 次提交
```

---

## T1.3 — 音频播放集成测试

```
### 任务：音频播放集成测试

### 当前状态
- test_audio.cpp 有 11 个用例，全是单元测试
- SoLoudAudioEngine 可独立 init，但需要真实音频文件
- tests/audio/silence.wav 已存在（测试用静音文件）
- 播放路径是相对路径，测试需从 build\tests\Debug 执行

### 执行

#### Step 1: 音频集成测试
- 创建 tests/cpp/test_audio_integration.cpp
- 测试用例（至少 5 个）：
  1. init → 加载 silence.wav → playSE → 等待 0.5s → stopSE（验证不崩溃）
  2. playBGM → 检查 isBGMPlaying() → stopBGM
  3. setGlobalVolume(0.5f) → 检查 getGlobalVolume() 返回 0.5
  4. playVoice → activeVoiceCount() ≥ 0
  5. fadeVolume bgm 1.0→0.0 在 1 秒内完成
  6. shutdown 后重新 init 成功（重新初始化）
- 注意：测试在 build\tests\Debug 目录执行，确保 silence.wav 可访问
- 更新 tests/CMakeLists.txt
- 构建 + 测试全绿后提交

### 预期产出
- tests/cpp/test_audio_integration.cpp（6+ 用例）
- 1 次提交
```

---

## T1.4 — 存档→读档往返测试

```
### 任务：存档→读档往返测试

### 当前状态
- test_save_migration.cpp 已存在（JSON 迁移测试）
- SaveManager 可独立 init（传入临时目录）
- 加密测试依赖 CryptoEngine（已通过 BackendRegistry 注册）

### 执行

#### Step 1: 存档往返测试
- 创建 tests/cpp/test_save_roundtrip.cpp
- 测试用例（至少 5 个）：
  1. save → load → 数据完整性（JSON 字段逐项比对）
  2. 不存在的 slot 返回空 JSON
  3. listSaves 返回正确的 slot 列表
  4. deleteSlot → slotExists 返回 false
  5. 加密保存（设置 key） → 读回 → 解密数据正确
  6. 错误 key 解密失败返回空 JSON
- 使用临时目录作为存储路径（不污染真实存档）
- 更新 tests/CMakeLists.txt
- 构建 + 测试全绿后提交

### 预期产出
- tests/cpp/test_save_roundtrip.cpp（6+ 用例）
- 1 次提交
```

---

## T1.5 — 热重载集成测试

```
### 任务：热重载集成测试

### 当前状态
- test_debug.cpp 存在（HotReload + DebugProtocol 测试）
- HotReload 依赖 scripts/ 目录存在且可写
- 测试目录：tests/scripts/ 下有多个 .lua/.ks 脚本

### 执行

#### Step 1: 热重载测试
- 创建 tests/cpp/test_hotreload_integration.cpp
- 测试用例（至少 3 个）：
  1. HotReload::init 正常启动
  2. 修改脚本文件 → 检测到变化 → 触发重载回调
  3. 重载后 Lua 状态正确（GameState 保留）
- 使用 tests/scripts/ 目录作为测试脚本目录
- 更新 tests/CMakeLists.txt
- 构建 + 测试全绿后提交

### 预期产出
- tests/cpp/test_hotreload_integration.cpp（3+ 用例）
- 1 次提交
```

---

## T1 验收总清单

- [ ] 5 个新测试文件
- [ ] 至少 28 个新 TEST_CASE
- [ ] 193 → 221+ 测试全绿
- [ ] cmake --build 零错误