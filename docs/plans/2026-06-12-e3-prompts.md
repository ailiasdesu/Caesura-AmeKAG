# E3 — script 模块完善提示词

## 通用规则

```
你是 Caesura (AmeKAG) 引擎开发者。先读 AGENTS.md。
一步一提交。构建零错误，测试全绿。
构建：cd build && cmake --build . --config Debug --parallel
测试：cd build\tests\Debug && .\CaesuraTests.exe
```

---

## 缺口

6 个测试文件 41 个用例。缺少 KAG 错误恢复、GameState 持久化、绑定参数校验的边界测试。

## Step 1: KAG 错误恢复测试

```
文件：tests/cpp/test_kag_execution.cpp（当前 12 用例）

新增 4 个用例：
1. "KAG: malformed script does not crash"
   → tokenizer.parse("@invalid @@syntax") 不崩溃，返回错误
2. "KAG: jump to nonexistent label does not crash"
   → @jump label_ghost（不存在）→ conductor 报错不崩溃
3. "KAG: choice with zero options degrades gracefully"
   → @choice（无参数）→ 解析不崩溃
4. "KAG: deeply nested if does not crash"
   → 15 层嵌套 @if/@endif → 解析成功 or 明确报错

实现要点：
- 使用内联 KAG 脚本字符串（不依赖外部 .ks 文件）
- 每个用例独立初始化 LuaManager
- 构建 + 测试全绿后提交
```

## Step 2: GameState 持久化测试

```
文件：tests/cpp/test_game_state.cpp（当前 5 用例）

新增 3 个用例：
1. "GameState: persist across save/load cycle"
   → push 数据 → 模拟存档 → 新 LuaManager 加载 → 读回数据一致
2. "GameState: nested table storage"
   → push {key={nested="value"}} → get 返回嵌套表
3. "GameState: clear then push isolates state"
   → push A → clear → push B → get A 返回 nil, get B 返回正确值

实现要点：
- GameState 持久化依赖 SaveManager 的 JSON 序列化
- 构造最小 JSON 存档验证往返
- 构建 + 测试全绿后提交
```

## Step 3: 绑定参数校验测试

```
文件：tests/cpp/test_script_bindings.cpp（当前 9 用例）

新增 5 个用例：
1. "Bindings: KAG play_bgm empty path does not crash"
   → lua_pcall play_bgm("") 返回 LUA_OK 或错误
2. "Bindings: Render load_texture empty path returns 0"
   → load_texture("") 返回 0，不崩溃
3. "Bindings: VFX create_emitter negative count returns -1"
   → particles_create_emitter({rate=-1}) 返回 -1
4. "Bindings: Debug log nil message does not crash"
   → Debug.log(nil) 不崩溃
5. "Bindings: DevCore set_dev_mode invalid value does not crash"
   → DevCore.set_dev_mode(999) 不崩溃

实现要点：
- 每个测试前初始化 BackendRegistry + registerNullBackends()
- 构建 + 测试全绿后提交
```

## 验收

- [ ] 306 → 318+ 测试全绿
- [ ] 12 个新边界用例覆盖错误路径
- [ ] 3 次提交