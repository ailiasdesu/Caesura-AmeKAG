# Phase 1 执行提示词

## 通用前置约束（每个 Agent 必须遵守）

```
你是 Caesura (AmeKAG) galgame 引擎的开发者。先读 AGENTS.md 理解核心架构约束。

关键规则：
- 所有后端访问走 BackendRegistry::instance().get*()
- 新接口放 src/<module>/api/I*.h，纯虚类
- 禁止跨模块引用具体头文件（组合根 entry/ 除外）
- 大小写：模块目录全小写，git mv 修正
- 一步一提交，构建零错误 + 149 测试全绿
- 构建命令：cd build && cmake --build . --config Debug --parallel
- 测试命令：cd build\tests\Debug && .\CaesuraTests.exe
```

---

## M1 — Steam 模块完善

```
## 任务：Steam 模块完善（M1）

### 当前状态
- src/steam/ISteamBackend.h — 接口在模块根目录（不在 api/）
- src/steam/NullSteamBackend.h — 桩实现
- src/steam/SteamBackend.h/.cpp — 已有部分实现
- 无 api/ 目录
- 无独立测试文件
- BackendRegistry 中已注册 ISteamBackend*

### 执行步骤

#### Step 1: 迁移接口到 api/
- 创建 src/steam/api/ 目录
- 移动 ISteamBackend.h → src/steam/api/ISteamBackend.h
- 更新所有 include 路径（BackendRegistry.h, Engine.cpp, main.cpp, NullSteamBackend.h, SteamBackend.h, SteamBinding.cpp, CloudSaveProvider.cpp, test_steam.cpp）
- 提交

#### Step 2: 规范 NullSteamBackend
- NullSteamBackend 继承 ISteamBackend，确保所有纯虚方法有实现
- 每个方法返回安全默认值（false, 0, 空字符串）
- 提交

#### Step 3: 检查 SteamBackend 实现完整性
- 读取 SteamBackend.h/.cpp，确认 Steamworks SDK API 调用正确
- 确认条件编译（#ifdef CAESURA_STEAM）包裹所有 SDK 引用
- 确认 shutdown 时正确释放 SDK 资源
- 提交

#### Step 4: 添加测试
- 创建 tests/cpp/test_steam.cpp
- 测试用例（至少 3 个）：
  1. NullSteamBackend 构造/名称/安全默认值
  2. SteamBackend 构造不崩溃（无 SDK 环境下）
  3. ISteamBackend 接口完整性检查
- 确保 tests/CMakeLists.txt 包含新文件
- 构建 + 测试全绿后提交

### 预期产出
- src/steam/api/ISteamBackend.h
- tests/cpp/test_steam.cpp
- 4 次提交
```

---

## M2 — Live2D 模块完善

```
## 任务：Live2D 模块完善（M2）

### 当前状态
- src/live2d/IAnimationBackend.h — 接口在模块根目录
- src/live2d/NullAnimationBackend.h/.cpp — 桩实现
- src/live2d/Live2D/Live2DBackend.h/.cpp — 有 Cubism SDK 实现
- src/live2d/Live2D/ 下有 4 个 RenderPath 实现（D3D11/Metal/OpenGL）
- api/ 目录已存在但为空
- 无测试文件
- BackendRegistry 中已注册 IAnimationBackend*

### 执行步骤

#### Step 1: 迁移接口到 api/
- 移动 IAnimationBackend.h → src/live2d/api/IAnimationBackend.h
- 更新所有 include 路径
- 提交

#### Step 2: 确认 NullAnimationBackend 完整性
- 确保所有 IAnimationBackend 纯虚方法有实现
- 确保在无 Cubism SDK 环境下编译通过
- 提交

#### Step 3: 检查 Live2DBackend 条件编译
- 确认所有 Cubism SDK 引用在 #ifdef CAESURA_LIVE2D 内
- 确认 CMakeLists.txt 中 Live2D 相关源文件条件编译正确
- 确认 4 个 RenderPath（D3D11/Metal/OpenGL）按平台条件编译
- 提交

#### Step 4: 添加测试
- 创建 tests/cpp/test_live2d.cpp
- 测试用例（至少 3 个）：
  1. NullAnimationBackend 构造/名称/安全默认值
  2. IAnimationBackend 接口完整性
  3. Live2DBackend 构造不崩溃（无 SDK 环境）
  4. 模型加载失败返回 nullptr 不崩溃
- 提交

### 预期产出
- src/live2d/api/IAnimationBackend.h
- tests/cpp/test_live2d.cpp
- 4 次提交
```

---

## M3 — MiniGame 模块完善

```
## 任务：MiniGame 模块完善（M3）

### 当前状态
- src/minigame/api/IMiniGameBackend.h — 接口已就位 ✅
- src/minigame/NullMiniGameBackend.h/.cpp — 桩实现
- src/minigame/BgfxMiniGameBackend.h/.cpp — bgfx 后端（部分实现）
- src/minigame/MiniGeometry.h/.cpp — 几何工具
- src/minigame/MiniCollision.h/.cpp — 碰撞检测
- src/minigame/MiniMaterial.h, MiniLight.h — 材质/光照
- 已有 tests/cpp/test_mini_game.cpp（但 59 skipped 中可能有它）
- BackendRegistry 中已注册 IMiniGameBackend*

### 关键发现
- test_mini_game.cpp 存在但测试结果在 "59 skipped" 中
- BgfxMiniGameBackend 包含场景加载、几何体、Shader 引用
- EmbeddedShaders_MiniGame.cpp 有专门的 minigame shader

### 执行步骤

#### Step 1: 确认 NullMiniGameBackend 完整性
- 检查所有 IMiniGameBackend 纯虚方法是否实现
- 确保每个方法用 printf 打印 "[MiniGame] Reserved: ..." 日志
- 提交

#### Step 2: 审查 BgfxMiniGameBackend
- 确认 init/shutdown 生命周期正确
- 确认 loadScene/enter/leave/render 链路完整
- 确认 bgfx 资源在 shutdown 时正确释放
- 确认不依赖任何非 api/ 的外部具体头文件
- 提交

#### Step 3: 启用 MiniGame 测试
- 读取 tests/cpp/test_mini_game.cpp，确认测试内容
- 修复任何导致测试被 skip 的问题（可能是缺少测试资源或编译条件）
- 确保测试在无 GPU 窗口环境下能运行（使用 NullMiniGameBackend）
- 添加额外测试用例：
  1. BgfxMiniGameBackend 构造不崩溃
  2. NullMiniGameBackend 所有方法返回安全值
  3. 场景加载/卸载无泄漏
- 构建 + 测试全绿后提交

### 预期产出
- test_mini_game.cpp 从 skipped 变为 passed
- 新增测试用例
- 3 次提交
```

---

## 执行检查清单

每个模块完成后确认：
- [ ] cmake --build . --config Debug 零错误
- [ ] 149 + 新增测试全部通过
- [ ] rg '#include.*"\.\./' src/<module>/ --iglob '*.h' | rg -v '/api/' 返回空（头文件无异模块具体依赖）
- [ ] git log --oneline -3 显示清晰的提交历史
