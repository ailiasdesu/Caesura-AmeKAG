---
title: Beta+→v1.0-rc 收尾 — 存档完善 + 文档补齐 + Demo 场景
type: feature
status: planned
date: 2026-06-10
origin: 全量代码审查完成 → 引擎 ~95% Beta+ → 收尾至 v1.0-rc
prerequisites: 编辑器全连通 (✅), MiniGame Shader (✅), FFmpeg 默认启用 (✅), 全量审查零违规 (✅)
---

# Beta+→v1.0-rc 收尾 — 存档完善 + 文档补齐 + Demo 场景

## Summary

引擎 ~95% Beta+。全量审查确认零核心约束违规、零内存泄漏、零 TDR。本计划收尾 4 项剩余工作至 v1.0-rc 发布就绪。

## Implementation Units

### IU-1: SaveManager 缩略图优化 (P2 → ✅)
- 当前：`captureThumbnailPNG` 用 `bgfx::requestScreenShot` 磁盘 I/O（功能可用）
- 目标：改为 `bgfx::readTexture` 内存读取 + stb_image_write 内存 PNG 编码
- 影响：存档速度提升 ~10x，避免磁盘碎片
- 文件：`src/System/SaveManager.cpp`

### IU-2: KAG API 文档补齐 (P2 → ✅)
- 当前：9 个章节覆盖 Layer/Text/Audio/System/Transition/VFX/Video/Live2D/沙箱
- 缺口：Resource（preload/cache）和 Save（save/load/listsaves）子模块未文档化
- 文本命令（ruby/font/skip/reset/pt/button 等）参数说明不完整
- 目标：补至 53/53 命令完整覆盖
- 文件：`docs/api/KAG-API.md`

### IU-3: Demo 场景扩展
- 当前：`scripts/demo.lua` + `scripts/demo/main.lua` 存在但可能覆盖不全
- 目标：新增场景覆盖 Live2D 模型加载、MiniGame 3D、视频播放、存档读档
- 文件：`scripts/demo.lua`, `scripts/demo/`

### IU-4: Live2D 纹理加载 TODO 闭合
- 当前：`Live2DBackend.cpp:227` 纹理加载 TODO（纹理已通过 `ILive2DRenderPath::createTexture` 创建，bgfxTex 已设置）
- 验证：确认当前实现已完整，移除 TODO 标记或补齐缺失步骤
- 文件：`src/Live2D/Live2D/Live2DBackend.cpp`

## Test Plan

- IU-1: 存档 → 缩略图 base64 非空 → 无磁盘临时文件残留
- IU-2: 逐命令对比 KAG-API.md ↔ `scripts/kag/commands/*.lua` function 列表
- IU-3: `--demo` 运行覆盖全部子系统无报错
- IU-4: Live2D 模型加载 → 纹理正确显示

## Assumptions

- 跨平台 CI 和 Live2D macOS/Linux 移交开发者，不在本计划范围
- MobileAdapter (P2) 和 CloudSync (P3) 留至 v1.1
- 编辑器 bug (光标 grabbing 等) 已在之前修复，无需重复
