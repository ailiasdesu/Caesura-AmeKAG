---
problem_type: quality_fixes
track: p1_p2_resolution
category: quality-issues
severity: resolved
status: completed
repo: Caesura (AmeKAG)
branch: master
date: 2026-06-07
resolved_by: spec-driven-fixes
total_issues_resolved: 6
---
# P1/P2 质量问题修复汇总 (2026-06-07)

## 修复依据

严格按照 `docs/Caesura_功能实现规格说明书_整合版.md` 对应章节规范修复。

---

## 已修复项

### Q3 (P1): Scheduler [switch]/[case]/[default] 功能存根 → 完整实现
- **文件**: `scripts/scheduler.lua`
- **规范**: [1.4] flow.lua 要求 [switch]/[case] 完整工作
- **改动**:
  - 新增 `[default]` 支持（无匹配 case 时走 default 分支）
  - 两阶段实现：Phase 1 扫描匹配 case body 范围 → Phase 2 在 body 内逐个 token yield 执行
  - 嵌套 switch 正确处理（depth 计数器）
  - case body 内每个 token 执行后 `coroutine.yield()` 保持协程模型
- **变更量**: +42 行（替代 18 行存根）

### Q5 (P1): 音频全局静态缓存 → 类成员封装
- **文件**: `src/Audio/SoLoudAudioEngine.h`, `SoLoudAudioEngine.cpp`
- **规范**: [3.1] audio.lua 总线管理 + [10.2.69] LRU 逐出
- **改动**:
  - 3 个全局静态变量 (`g_waveCache/g_waveLRU/g_waveLRUMap`) → 类私有成员 (`m_waveCache/m_waveLRU/m_waveLRUMap`)
  - `loadWave()` 从自由函数 → `SoLoudAudioEngine::loadWave()` 成员方法
  - `isStreamFormat()` 保留为文件作用域 static（无状态辅助函数）
  - `flushWaveCache()` 的 shutdown 清理由成员缓存正确执行
- **收益**: 可测试性提升，缓存生命周期与 SoLoudAudioEngine 实例绑定

### Q7 (P1): Core ../ 相对路径 → 规范模块路径
- **文件**: `src/Core/Engine.cpp`
- **改动**: 6 处 `../Render/`、`../Audio/`、`../Scripting/`、`../System/`、`../Resource/`、`../Debug/` → 规范为 `Render/`、`Audio/` 等
- **收益**: CMake `target_include_directories(src)` 已包含 src/，路径一致性提升

### Q9 (P2): `-p` 目录清理
- **操作**: `Remove-Item -Recurse -Force "-p"`
- **状态**: 已删除

### Q10-Q11 (P2): .gitignore 完整性验证
- **结果**: `build/`, `logs/`, `saves/`, `settings/`, `config/` 均已忽略
- **状态**: 配置正确，无需修改

### Q8 (P2): Scheduler.resume() 与 .run() 关系文档化
- **文件**: `scripts/scheduler.lua`
- **规范**: [1.2] 协程恢复模型
- **现状**: `resume()` 转发到 `run()` 是正确设计——两者参数签名不同（外部 resume vs 内部 run），无需合并
- **操作**: 保留现状，文档化设计意图

---

## 标记为已知（Beta 处理）

| 问题 | 原因 |
|------|------|
| Q6 命名空间 `caesura::carc` vs `Caesura` | Beta 阶段统一（影响 11 个文件 + 所有消费者） |
| Q12 CryptoEngine Linux/macOS 抽象 | Beta 阶段实现平台抽象层 |
| Q13 Tokenizer 82/167 | ALPHA_NOTES 已知 #5 |
| Q14 VideoPlayer FFmpeg | CMake option `CAESURA_VIDEO_FFMPEG=OFF` |

---

## 构建验证

| 配置 | 状态 |
|------|:---:|
| Debug | ✓ 通过 |
| Release | ✓ 通过 |

---

## 变更统计

| 文件 | 行变更 |
|------|:---:|
| `scripts/scheduler.lua` | +42 / -18 |
| `src/Audio/SoLoudAudioEngine.h` | +8 / -0 |
| `src/Audio/SoLoudAudioEngine.cpp` | 重构（全局 → 成员） |
| `src/Core/Engine.cpp` | 6 处路径修正 |
| `-p` 目录 | 删除 |
| **合计** | ~80 行 |
