# Caesura (AmeKAG) 引擎现有能力清单

> 2026-06-07 | Alpha 阶段 | C++ 642KB / Lua 374KB | 95 C++ 文件 / 42 Lua 文件 / 18 测试文件
> 说明书决策实现度: P0 12/13 · P1 28/30 · P2 13/14 · 整体 93%

---

## 一、脚本引擎 (Part 1)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| KAG 3.0 语法解析 | ✅ | `tokenizer.lua` — LPeg 通用 `[cmd params]` 语法，支持全部 KAG 标签 |
| 协程调度 | ✅ | `scheduler.lua` — 遍历 token 序列，`kag[cmd](ctx,params)` 分发，`coroutine.yield()` 阻塞 |
| 流程控制 | ✅ | `[if]/[else]/[endif]` 条件分支 + 嵌套；`[switch]/[case]/[default]/[endswitch]` 多路分支 |
| 子程序调用 | ✅ | `[call]/[return]` 跨场景跳转，`call_stack` 保存上下文 |
| 宏系统 | ✅ | `[macro]/[endmacro]` 参数替换 + 内联展开 |
| 标签系统 | ✅ | `*label` + `[jump]` 同场景跳转 |
| CancelToken | ✅ | 两阶段取消模型：`mark_cancelled()` + `execute_callbacks()`，Lua 5.4 `__close` 自动清理 |
| Lua 5.4 沙箱 | ✅ | `sandbox.lua` — `_ENV` 白名单 (KAG/Render/math/string/table)，Dev/Release 双模式 |
| GameState | ✅ | `ctx` 统一传递：co / tokens / token_index / call_stack / f / sf / tf / layers / backlog / active_operations / stop_flag |
| kag 命令 | ✅ | 46 个已实现命令（详见说明书 Part 1） |
| 场景加载 | ✅ | `flow.lua` — `load_scene()` 跨文件加载 + `scene_cache` 缓存 |
| [eval]/[emb] | ✅ | 脚本内嵌 Lua 表达式执行，受沙箱约束 |

> ⚠️ 缺失: `coroutine.close()` 协程关闭守卫 `[10.2.21]`

---

## 二、图形渲染 (Part 2)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| bgfx 后端 | ✅ | Vulkan 1.1+ / D3D11 / Metal，`BgfxRenderDevice` 封装 |
| 图层系统 | ✅ | bg(0) / fg(1) / message(2) 三层 z-order，`LayerManager` 管理 |
| 28 种混合模式 | ✅ | W3C Compositing Level 1 + Porter-Duff，`fs_blend.sc` 单 switch 实现 |
| 转场动画 | ✅ | Rule Image + progress + easing (linear/ease-in/ease-out/ease-in-out) |
| 仿射变换 | ✅ | 移动/缩放/旋转，4 种重采样 |
| 视觉效果 | ✅ | Quake / Blur / Fade，`vfx.lua` + `VFXBinding` |
| 实时调色板 | ✅ | `palette.lua` + `fs_palette.sc`，H/S/L/V 实时调整 |
| CompositeShaderCache | ✅ | Blend+Palette 单 Pass 合并，`CompositeShaderKey` hash 去重 |
| LUT 纹理优化 | ✅ | 单纹理 + progress uniform，禁止每帧创建 |
| 脏矩形 Scissor | ✅ | union > 75% 退化全量，单矩形 bgfx setScissor |
| 屏幕适配 | ✅ | letterbox / stretch / crop + Resize RTT 重分配 |
| 嵌入着色器 | ✅ | DX11 HLSL / GLSL / Metal 三套预编译，`EmbeddedShaders_SPIRV` |
| TextureArray 字体图集 | ✅ | 预分配 4 层 2048x2048 RGBA8，texture2DArray shader |
| CJK 静态图集 fallback | ✅ | 内置 3500 汉字 Noto Sans CJK SC，~15MB，扩容时无缝降级 |

---

## 三、音频系统 (Part 3)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| SoLoud 引擎 | ✅ | `SoLoudAudioEngine` 完整封装 |
| BGM Bus | ✅ | 单流播放，fadeVolume / xfade 交叉淡入淡出 |
| Voice Bus | ✅ | 单流 + 打断模式，`playvoice` 回调唤醒 Lua 协程 |
| SE Bus 池 | ✅ | 8 路并行，per-handle `setHandleVolume` |
| 混合提交模型 | ✅ | 非阻塞 batch（帧末提交）/ 阻塞直调（voice），混用检测 + Debug 抛错 |
| 音视频同步 | ✅ | `SoLoud::getAudioPosition` → PTS，追赶/丢帧 ±1 帧窗口 |
| 空后端 | ✅ | `NullAudioBackend` 无音频环境可用 |
| 线程安全 | ✅ | `CAESURA_ASSERT_MAIN_THREAD()` 主线程独占守护 |

---

## 四、系统设施 (Part 4 + Part 7)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| JSON 存档 | ✅ | `SaveManager` — `schema_version` + `migration chain` 跨版本迁移 |
| 存档缩略图 | ✅ | 可配置分辨率/质量，自适应压缩流水线 |
| 配置系统 | ✅ | `config.lua` + `settings.lua` — text_speed / design / adapt / volume / thumbnail |
| i18n 国际化 | ✅ | `i18n.lua` — `_T` 翻译表 + `%{key}` 插值，Backlog 存 key 实时翻译 |
| 历史回顾 | ✅ | `history_ui.lua` — Backlog 滚动显示，支持跳转到场景 + 语音重播 |
| 脚本热重载 | ✅ | `HotReload.cpp` — ScriptState 状态机（IDLE / DEBUG_ACTIVE / RELOADING），句柄有效性检查 |
| 调试协议 | ✅ | `DebugProtocol.cpp` — `lua_sethook` 断点/单步，`DebugBinding` C++ 绑定 |
| 错误界面 | ✅ | `ErrorUI.cpp` — 硬编码几何 + 内置 8x16 位图字体，Retry / Title / Quit 三选项，Title 连续崩溃 3 次 → SDL MessageBox fallback |
| 内存管理 | ✅ | `TextureBudget.cpp` — 纹理 1GB 预算 + 对象池；Lua 256MB 硬上限 + 显式 GC 点 |
| 输入路由 | ✅ | `InputRouter.cpp` — SDL3 事件 → KAG 命令映射 |
| 平台适配 | ✅ | `SDL3PlatformBackend` + `MobileAdapter` + `BackendRegistry` |
| 异步加载 | ✅ | `AsyncLoader` — 16 槽队列 + `createTextureAsync` + `[preload]` 标签 + SDL_PushEvent 回调 |
| 异步回退 | ✅ | 加载中显示棋盘格占位，完成后即时替换 |

---

## 五、视频与字体 (Part 5)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| MPEG-1 视频 | ✅ | `VideoPlayer` — `pl_mpeg` 解码，逐帧纹理绑定 + PTS 同步 |
| FFmpeg 视频 | ✅ | `IVideoDecoder` 接口预留，`#ifdef CAESURA_VIDEO_FFMPEG` 条件编译 H.264/VP9/WebM |
| 视频图层融合 | ✅ | 解码帧作为 `bgfx::TextureHandle` 绑定图层，与其他图层一致的 quad 管线 |
| FreeType 矢量字体 | ✅ | `FontRenderer` + `FreeTypeContext`，TTF/OTF 实时渲染 |
| 文本布局引擎 | ✅ | `TextRenderer` — 自动换行 / H/V 对齐 / 行间距 / 中英混排 / ruby 注音 |
| 文本批处理 | ✅ | `MessageLayerCache` — 顶点缓冲缓存，单 `bgfx::submit()`，O(1) draw call |
| 动态图集扩容 | ✅ | A/B 双 TextureArray + CJK 静态图集 fallback，扩容期间暂停文字渲染但不暂停翻页 |

---

## 六、3D 小游戏接口 (Part 6)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| RTT 画布 | ✅ | `RTTManager` — `acquireCanvas()` / `releaseCanvas()` pool 机制 + 延迟销毁队列 |
| 2D/3D 类型区分 | ✅ | `RT_2D` / `RT_3D` 枚举，独立 pool |
| 视口清除控制 | ✅ | `create_viewport({clear_color, clear_depth, no_clear})` |
| 3D/2D 混合 | ✅ | RTT → 普通纹理 → bg/fg 图层绑定 + 混合模式 |
| Lua 接口 | ✅ | `rtt.lua` — `create_viewport()` / `destroy_viewport()` / `get_viewport_texture()` |
| 生命周期 | ✅ | Lua `__gc` → `destroyViewport`，C++ `shared_ptr` |

> ⚠️ 缺失: 着色器上限 64KB + Vertex 48B 安全检查 `[10.2.3]`

---

## 七、粒子系统

| 能力 | 实现 | 说明 |
|------|:---:|------|
| 粒子发射器 | ✅ | `ParticleSystem` — `Emitter` 配置 + `createEmitter()` / `destroyEmitter()` |
| 粒子模拟 | ✅ | `update(dt)` — 位置/速度/生命期/颜色衰减 |
| GPU 提交 | ✅ | `bgfx::TransientVertexBuffer` + `bgfx::submit()` |

---

## 八、CARC 容器格式 (Part 10)

| 能力 | 实现 | 说明 |
|------|:---:|------|
| AES-256-GCM 加密 | ✅ | `CryptoEngine` — BCrypt 实现，256-bit 密钥 + 96-bit nonce |
| Ed25519 签名 | ✅ | orlp/ed25519 库，64B 签名覆盖 Header + Content + Index |
| 文件布局 | ✅ | Header(64B) + Content + Index(encrypted) + Signature(96B) + PublicKey(32B) |
| 公钥外置 | ✅ | 引擎启动时读取 CARC 末尾 32B 公钥，无需重编译 |
| 签名版本防降级 | ✅ | SHA-256(header‖version‖count‖hashes)，MIN/MAX 版本检查 |
| 链式信任 | ✅ | `CRLManager` + 证书吊销列表，子 CARC 需授权证书 |
| Provider Chain | ✅ | 责任链：CARC(10) > Patch(8) > Dir(5)，`IAssetProvider` 抽象接口 |
| CARC 读写 | ✅ | `CARCReader` / `CARCWriter` — 打包/解包 + 流式读取 |
| Lua 字节码预编译 | ✅ | `.lua` → `luac` 字节码存储于 CARC |
| 构建工具 | ✅ | `tools/carc_pack/carc_pack.exe` |

> ⚠️ 缺失: `bsdiff` 差分更新 + 原子回滚 `[10.2.16]`

---

## 九、质量保证

| 能力 | 实现 | 说明 |
|------|:---:|------|
| 单元测试 | ✅ | 18 个测试文件，doctest 框架 |
| GpuMonitor | ✅ | 帧预算 16.67ms @ 60fps，rolling average + 过载检测 |
| CAESURA_ASSERT | ✅ | Debug 构建 abort，Release 构建 `((void)0)` 零开销 |
| 错误恢复 | ✅ | 全局 `kag.try()` 包裹，3 层 fallback (Retry→Title→SDL MessageBox) |

---

## 十、未实现（Alpha 阶段 4 项缺口）

| # | 决策 | 严重度 | 预计工作量 |
|:--:|------|:---:|:---:|
| 1 | `[10.2.3]` 3D 着色器/顶点安全约束 | P0 | 30 行 C++ |
| 2 | `[10.2.21]` coroutine.close() 守卫 | P1 | 10 行 Lua |
| 3 | `[10.2.16]` CARC bsdiff 差分回滚 | P2 | 200 行 C++ |
| 4 | `[10.2.44]` FreeType 扩容 spinlock | P2 | 40 行 C++ |

---

## 十一、Beta 路线图（Part 8 — 0% 实现）

| 能力 | 状态 |
|------|:--:|
| Fiber-based Job System | ❌ |
| Work-Stealing 线程池 | ❌ |
| 三区边界模型 (红/绿/黄) | ❌ |
| AssetPipeline 统一加载 | ❌ |
| 纹理加载四阶段 pipeline 并行化 | ❌ |
| 音频波形预解码并行化 | ❌ |
| 粒子模拟并行化 | ❌ |
| 字体 glyph 并行光栅化 | ❌ |
| 存档 JSON 序列化并行化 | ❌ |

---

## 附录: 外部依赖

| 库 | 版本 | 用途 | 许可证 |
|----|------|------|:---:|
| bgfx | latest | 跨平台渲染 | BSD-2 |
| Lua | 5.4 | 脚本引擎 | MIT |
| LPeg | 1.1 | KAG 语法解析 | MIT |
| SoLoud | latest | 音频引擎 | zlib |
| SDL3 | 3.2 | 窗口/输入/事件 | zlib |
| FreeType | 2.13 | 矢量字体 | FTL |
| zstd | latest | CARC 压缩 | BSD |
| pl_mpeg | latest | MPEG-1 解码 | MIT |
| orlp/ed25519 | latest | Ed25519 签名 | CC0 |
| stb_image | latest | 图片解码 | MIT / Public Domain |
| doctest | latest | 单元测试 | MIT |